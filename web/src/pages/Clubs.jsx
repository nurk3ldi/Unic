import './Page.css';

/** Список клубов. Пока только заголовок — наполним на следующем шаге. */
export default function Clubs() {
  return (
    <main className="page">
      <h1 className="page__title">Клубы</h1>
    </main>
  );
}
