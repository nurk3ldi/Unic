# Unic — университет клубтарын басқару жүйесі

> Бұл файл — жобаның конституциясы. Әр сессияда оқылады. Ережелер міндетті.

## 1. Жоба туралы

**Unic** — университет клубтарын басқаруға арналған веб-қосымша: клубтар каталогы,
мүшелік, іс-шаралар, рөлге негізделген басқару.

- **Интерфейс тілі: тек орысша.** UI-дағы барлық мәтін, батырма, қате хабарламасы,
  валидация, placeholder — 100% орысша. i18n қабаты жоқ (әзірге қажет емес).
- **Тақырып: тек светлая тема.** Dark mode жоқ. `prefers-color-scheme: dark` өңделмейді.
- **Дизайн: Apple.** Толық ережелер — §4.

## 2. Стек (өзгертпеу, жаңа тәуелділік қоспау)

| Қабат | Таңдау | Себебі |
|---|---|---|
| Frontend | React 19 + Vite | Жылдам dev, HMR |
| Роутинг | react-router-dom | Стандарт |
| Стильдер | **Таза CSS** (`.css` файлдар + CSS айнымалылары) | Tailwind/CSS-in-JS ЖОҚ |
| Иконкалар | **`react-icons/io5` (Ionicons)** | iOS стилінде сызылған, SF Symbols-ке ең жақын ашық кітапхана. **lucide-react ҚОЛДАНУҒА БОЛМАЙДЫ** |
| Backend | Node.js + Express 5 | — |
| БД | PostgreSQL 18 (жергілікті) + `pg` драйвері | ORM жоқ — таза SQL |
| Auth | JWT (`jsonwebtoken`) httpOnly cookie-де + `node:crypto` scrypt | Пароль хэші stdlib-те, bcrypt қажет емес |
| Почта | `nodemailer` + Gmail SMTP | Қалпына келтіру кодын жіберу. SMTP бапталмаса — код серверлік консольге шығады |
| Env | `node --env-file=.env` | dotenv пакеті қажет емес (Node 20+ stdlib) |

**Тәуелділіктер тізімі жабық.** Жаңасын қосар алдында сұрау керек.

## 3. Құрылым

```
Unic/
├─ CLAUDE.md          ← осы файл
├─ package.json       ← npm workspaces, `npm run dev` екеуін де қосады
├─ server/
│  ├─ src/index.js    ← Express қосылу нүктесі
│  ├─ src/db.js       ← pg Pool
│  ├─ src/auth.js     ← scrypt хэш + JWT
│  ├─ src/middleware/auth.js
│  ├─ src/routes/     ← auth.js, clubs.js (құрам, чат, іс-шара), chats.js, events.js, invites.js
│  ├─ sql/schema.sql  ← БД схемасы (жалғыз ақиқат көзі)
│  └─ sql/seed.js     ← демо деректер
└─ web/
   ├─ index.html        ← Inter шрифті (Google Fonts), favicon сілтемесі
   ├─ public/favicon.png ← ақ squircle + көк логотип, 256×256, 15 KB
   ├─ vite.config.js    ← порт 5300, proxy → 4400
   └─ src/
      ├─ main.jsx       ← React root
      ├─ App.jsx        ← маршруттар мен қорғау (гость / кірген)
      ├─ AuthContext.jsx ← сессия: user, ready, setUser, signOut
      ├─ api.js         ← барлық fetch осы жерде
      ├─ photo.js       ← сурет → data URL: клубқа 400×400 квадрат, чатқа пропорциямен ≤1280px
      ├─ people.js      ← адам қалай көрсетіледі (қысқа аты, аватар әрпі, түсі, нөмірі, рөл атауы)
      ├─ chat.js        ← чаттың опрос аралығы, уақыт пішімі, күн бөлгіші, сілтеме шаблоны
      ├─ events.js      ← іс-шара уақытының пішімі (күнтізбе үшін)
      ├─ club.js        ← клуб күйлері мен «участник» септеуі (ортақ)
      ├─ index.css      ← токендер + reset
      ├─ assets/logo.png ← 480×213, мөлдір фон, 42 KB
      ├─ assets/chat-background.webp ← чат тұсқағазы, 2294×1290, WebP lossless
      ├─ components/    ← AuthCard, Field, PasswordField, Header, AppLayout,
                        ClubCard, ClubFormModal, MembersPanel, ClubControls,
                        ClubChatCard, ChatRoom, SearchField,
                        PhotoViewer, ChatNotifier, AccountSecurity,
                        Person, InviteDialog, InvitesBell
      └─ pages/         ← Home, Clubs, ClubPage, Chats, Events, Profile, Login, Register, Restore
                        + Auth.css (формалар), Page.css (ішкі экрандар)
```

## 4. Дизайн ережелері (Apple) — 100% сақталады

Негізі: `/apple-design` скиллі (WWDC «Designing Fluid Interfaces», «Principles of Great Design»).

### 4.1 Палитра (`:root`-тағы токенмен ғана қолданылады, hardcode ЖОҚ)

Қазір токендер `web/src/index.css` ішінде. Жоба өскенде `styles/tokens.css`-ке бөлінеді.

| Токен | Мән | Қолданысы |
|---|---|---|
| `--bg` | `#FFFFFF` | **Логотиптің фоны** (`design/logo.png`) — бүкіл жобаның фоны |
| `--bg-secondary` | `#F5F5F7` | Топталған беттер фоны (Apple grouped) |
| `--surface` | `#FFFFFF` | Карточка, панель |
| `--accent` | `#0063FD` | **Логотиптің көгі** (`design/logo.png`, пиксельден өлшенген). Ақ фонда 5.0:1 — WCAG AA ✓ |
| `--accent-hover` | `#1A73FF` | Hover (акценттен: ашықтық +5%) |
| `--accent-active` | `#0058E0` | Press (акценттен: ашықтық −5%) |
| `--accent-soft` | `rgba(0,99,253,0.1)` | Акценттің жұмсақ фоны (badge, белгі) |
| `--text-on-accent` | `#FFFFFF` | Акцент фонындағы мәтін мен иконка |
| `--shadow-raised` | екі қабатты, тереңірек | Модаль терезе |
| `--scrim` | `rgba(0,0,0,0.32)` | Модаль астындағы күңгірттеу |
| `--header-bg` | `rgba(239,244,255,0.85)` | **Сайт панелі: аққа жақын, бренд реңкімен** |
| `--header-fg` | `--text-secondary` | Панельдегі иконка (логотип — бренд көгі) |
| `--header-fill` | `--fill` | Панельдегі hover |
| `--header-height` | `3.5rem` | Панель биіктігі |
| `--club-size` | `calc((100dvh - --header-height) / 2)` | Клуб карточкасының ені = фото квадратының қыры |
| `--club-header-height` | `4.4375rem` | Карточка header-і (бекітілген — түйме өлшемі соған сүйенеді) |
| `--radius-widget` | `12px` | Виджет-карточкалар (клуб) |
| `--text` | `#1D1D1F` | Негізгі мәтін (Apple label) |
| `--text-secondary` | `#6E6E73` | Екінші деңгей |
| `--text-tertiary` | `#86868B` | Үшінші деңгей, placeholder |
| `--separator` | `rgba(0,0,0,0.08)` | Бөлгіш сызық |
| `--red` / `--green` | `#FF3B30` / `#34C759` | Қате / сәтті |
| `--green-soft` / `--green-strong` | `rgba(52,199,89,.14)` / `#1F7A3D` | Жасыл астар және оның үстіндегі мәтін (✓ түймесі) |

> Акцент — брендтен, Apple палитрасынан емес. `#007AFF` (iOS systemBlue) және `#0071E3`
> (apple.com) **қолданылмайды**. Логотип көгі олардан жақсы: ақ фонда 5.0:1, яғни мәтін
> ретінде де, ақ мәтінді түйме фоны ретінде де AA өтеді.
> `logo_text.png` көгі `#005FFB` — сол түс, компрессия айырмасы; канон `logo.png`-тікі.

### 4.2 Типографика
- **Шрифт: Inter** (Google Fonts, 400/500/600) — біреу ғана:
  `'Inter', -apple-system, BlinkMacSystemFont, Helvetica, Arial, sans-serif`.
  Себебі: SF Pro-ның ашық баламасы (Apple бағытына сәйкес), кириллицасы арнайы
  жобаланған, биік x-height пен ашық апертура — ұсақ өлшемде анық, сандары тураланған.
  Сыналған баламалар: Onest (жылырақ), Nunito, M PLUS Rounded 1c (дөңгелек).
  Helvetica Rounded алынбады — коммерциялық, бұл машинада Helvetica да жоқ (тек Arial).
- **Tracking өлшемге тәуелді:** ірі тақырып `-0.02em`, body `0`, ұсақ мәтін `+0.01em`.
- **Leading кері пропорционал:** display `1.05–1.1`, body `1.47`.
- Иерархия = өлшем + салмақ + leading жиынтығы, тек өлшем емес.
- Барлық аралық `rem`-де (px емес) — пайдаланушының шрифт өлшемін құрметтейміз.

### 4.3 Қозғалыс
- **Жауап pointer-down-та**, `click`-те емес. Press: `scale(0.97)`, `100ms`.
- Әдепкі spring: **damping 1.0** (overshoot жоқ), response `0.3–0.4s`.
- Bounce (damping ~0.8) **тек қимыл инерциясы болғанда** (flick, drag release).
- Анимация үзілмелі болуы керек: ағымдағы (presentation) мәннен басталады.
- Тек `transform` және `opacity` анимацияланады.
- Кіру мен шығу **бір жолмен** (симметрия). Popover `transform-origin` — триггерде.
- `prefers-reduced-motion` міндетті түрде өңделеді (cross-fade-ке ауысады).

### 4.4 Экрандар арасындағы ауысу
- **View Transitions API** (браузердің өз мүмкіндігі, кітапхана ЖОҚ). Сілтемеде
  `<Link to="..." viewTransition>`, стилі `index.css`-тегі `::view-transition-old/new(root)`.
- Ескі экран артқа шегінеді (`scale 0.97`, 260ms), жаңасы алдыға шығады
  (`scale 1.02 → 1`, 380ms) — қабат ауысқаны оқылады, сурет алмасқаны емес.
- `prefers-reduced-motion` кезінде қозғалыс алынады, бірақ ауысудың өзі қалады:
  қысқа еру (160ms). Firefox қолдамайды — сонда жай навигация болады (сынбайды).
- Карточка ішіндегі мазмұн **кезекпен** шығады (40 / 90 / 140 / 190ms):
  логотип → тақырып → форма → footer. Карточканың өзі материал ретінде келеді
  (`scale 0.97` + eру, 420ms).
- Микроауысулар: көз иконкасы мен түйме жазуы `key` ауысқанда қысқа масштабпен
  проявляется (220–240ms) — секіру емес, қозғалыс.

### 4.5 Формалар
- **Жазу өріс ішінде** (placeholder: «Почта», «Пароль»), сыртқы label жоқ.
  Семантика үшін `<label class="visually-hidden">` қалады — скринридер оқиды.
- **Фокуста ореол (box-shadow ring) ЖОҚ.** Тек шекара түсі жұмсақ ауысады:
  `transition: border-color 250ms var(--ease-out)`. Қате күйінде де солай — қызыл шекара.
  Глобалды `:focus-visible` сақинасы өріс ішінде өшіріледі (шекара фокусты көрсетіп тұр).
- Өлшемдер: өріс пен түйме биіктігі **48px** (бірдей), радиус `--radius-control` = **12px**.
  48px — Apple-дің 44px минималды тию аймағынан жоғары, бірақ форма ауыр көрінбейді.
- **Chrome autofill фоны басылады.** Браузер автотолтырғанда өрісті өзінің көгілдіріне
  бояйды әрі `background-color`-ға көнбейді — `Field.css`-те ол `box-shadow: inset`-пен
  жабылады, мәтін түсі `-webkit-text-fill-color` арқылы бекітіледі. Жаңа өріс жасағанда
  осы ереже қайталануы керек (немесе `Field` компоненті қолданылады).
- Өрістер астындағы жол: **солда «Запомнить меня» галочкасы, оңда «Забыли пароль?»**
  (`justify-content: space-between`). Галочка — жүйелік `<input type="checkbox">`,
  `accent-color: var(--accent)` арқылы боялған (кастом компонент жазылмайды).

### 4.6 Материалдар
- Навигация/toolbar — **транслюцентті қабат**: `background: rgba(255,255,255,0.72)` + `backdrop-filter: blur(20px) saturate(180%)`, контент астынан ағып өтеді.
- Транслюцентті бетке транслюцентті бет қойылмайды.
- Қатты 1px бөлгіштің орнына — scroll edge эффектісі (градиент маска).
- Радиустар: карточка `12px`, түйме `10px`, pill `980px`, модаль `16px`.
- Көлеңке жұмсақ әрі сирек: `0 1px 3px rgba(0,0,0,0.06), 0 8px 24px rgba(0,0,0,0.06)`.

### 4.7 Принциптер
Purpose · Agency · Responsibility · Familiarity · Flexibility · Simplicity · Craft · Delight.
- Нақты атаулар жалпыға қарағанда жақсы («Мои клубы», «Заявки» — «Главная» емес).
- Әр экран жауап береді: Мен қайдамын? Қайда бара аламын? Қалай шығамын?
- Валидация — inline, submit-те емес. Қате хабары нақты әрі орысша.
- Деструктив әрекетке ғана растау; қалғанына undo.

## 5. Код ережелері

1. **Қарапайымдық бірінші.** Спекулятивтік абстракция жоқ. Мәселені шешетін ең аз код.
2. **Хирургиялық өзгеріс.** Тек қажеттісіне тию, қатар тұрған кодты рефакторлау жоқ.
3. **Қайталама жоқ.** Жаңа компонент жазбас бұрын `components/` ішін тексеру.
4. **Түс/аралық/радиус hardcode жазылмайды** — токеннен `var()` арқылы.
5. Backend: барлық жауап JSON. Қате форматы: `{ "error": "Сообщение по-русски" }`.
6. Frontend: барлық сұраныс `api.js` арқылы. Компонент ішінде `fetch` жоқ.
7. SQL — параметрленген (`$1, $2`). Конкатенация ЖОҚ.
8. Пароль ешқашан логқа/жауапқа түспейді.

## 6. Іске қосу

```bash
npm install          # root + workspaces
npm run db:setup     # схема + демо деректер
npm run dev          # server :4400 + web :5300 (қатар)
```
Vite `/api` сұраныстарын `:4400`-ге проксилейді — CORS қажет емес.

### Хат шаблоны
- Файл: `server/src/mailer.js`. Тақырыпта **код болмайды** («Код для восстановления пароля») —
  тақырып құлып экранында және хабарландыруда көрінеді, код онда жатпауы керек.
- Оформление хаттың өз `style` атрибуттарында ғана (пошта клиенттері сыртқы CSS оқымайды),
  ені `<table>`-мен ұсталады — тек ол барлық жерде бірдей ортаға тұрады.
- Логотип **тіркеме (CID)** ретінде жіберіледі (`server/assets/logo.png`, 300px, 23 KB) —
  сыртқы хостинг қажет емес, Gmail оны блоктамайды.
- Дизайн беттермен бір тілде: `#F5F5F7` фон, ақ карточка (radius 18px), код `#0063FD`
  түсімен сұр қорапта, астында 10 минуттық ескерту. **Подвал жоқ** — хат кодпен және
  ескертумен аяқталады (пайдаланушының шешімі).
- Мәтіндік (`text`) нұсқасы да бар — HTML өшірілген клиенттер үшін.

### Почтаны баптау (қалпына келтіру коды)
`server/.env` ішіне Gmail деректерін жазу керек (Google аккаунтта 2FA қосулы болуы шарт,
пароль — қарапайым емес, «қосымша қолданба паролі»):
```
SMTP_USER=сіздің_адрес@gmail.com
SMTP_PASS=қосымша_қолданба_паролі
MAIL_FROM=Unic <сіздің_адрес@gmail.com>
```
Бос болса жүйе сынбайды — код серверлік консольге шығады.
**Маңызды:** `.env` өзгерген соң серверді қолмен қайта қосу керек (`Ctrl+C` → `npm run dev`),
себебі `node --watch` тек импортталған модульдерді бақылайды, `.env`-ті емес.

### Dev серверді пайдаланушы өз терминалында ұстайды
Claude ол процеске перне жібере алмайды (бөлек интерактивті консоль). Сондықтан:
- `src/**` өзгерісі → Vite HMR өзі қабылдайды, ештеңе істеудің қажеті жоқ.
- `index.html` немесе `vite.config.js` өзгерісі → Vite өзі restart жасайды.
- `public/**` (favicon, статикалық файл) → сервер бірден жаңасын береді, бірақ **браузер
  кэштеп қалады**: пайдаланушыға `Ctrl+Shift+R` басуды айту керек (Vite restart көмектеспейді).
- Жаңа тәуелділік орнатылса, **файлдар өшірілсе/көшірілсе немесе жаңа модуль құрылымы
  пайда болса** → restart керек: пайдаланушыға «терминалда `r` + Enter басыңыз» деп
  **нақты айту**.
- **`cat > file` қаупі:** ол файлды алдымен нөлге түсіреді. Vite watcher дәл сол сәтте
  оқып қалса, **бос модуль кэште қалады** да, бет ақ болады. Белгісі: `#root` бос,
  ал `curl :5300/src/.../File.jsx` жауабында `sourcesContent: [""]`.
  Емі — файлды **қайта сақтау** (оқып, сол күйінде жазу); restart қажет емес.
  Сондықтан файл жазуға `Write` құралын не Python-ды қолданған дұрыс. (2026-09-20-да тексерілді: Login.css өшіп, `components/` пайда болғанда
  HMR ескі модульді ұстап қалып, бет ақ болып қалды. Таза процесте код мінсіз жұмыс істеді.)
Claude серверді өзі өлтіріп қайта қоспайды — пайдаланушының логтары жоғалады.

## 6а. Маршруттар

| Жол | Экран | Қолжетімділік |
|---|---|---|
| `/` | `Home` (әзірге бос) | Тек кірген. Гость → `/login` |
| `/clubs` | `Clubs` (карточкалар торы, БД-дан) | Тек кірген |
| `/clubs/:id` | `ClubPage` (мәліметтер, басқару, чат, іс-шаралар) | Тек кірген |
| `/chats` | `Chats` (солда тізім, оңда таңдау сұралады) | Тек кірген |
| `/chats/:id` | `Chats` (солда тізім, оңда сол клубтың чаты) | Чат — клуб мүшелеріне |
| `/events` | `Events` (солда клубтар жолағы, оңда ай күнтізбесі) | Тек кірген |
| `/profile` | `Profile` (өз деректерің, шығу) | Тек кірген |
| `/login` | Кіру | Тек гость. Кірген → `/` |
| `/register` | Тіркелу | Тек гость |
| `/restore` | Парольді қалпына келтіру | Тек гость |

Сессия `AuthContext`-те: жүктелгенде `GET /api/auth/me` арқылы қалпына келеді.
`ready` болмайынша ештеңе рендерленбейді — экран жыпылықтамас үшін.
**Кіру де, тіркелу де бірден `/`-ке апарады** (сервер cookie-ді сол жауапта береді,
қосымша логин қажет емес). Пароль ауысқан соң — `/login`-ге.

## 7. Рөлдер

| Рөл | Код | Мүмкіндігі |
|---|---|---|
| Админ | `admin` | Жүйенің толық бақылауы |
| Университет | `university` | Клубтарды бекіту, статистика |
| Клуб басшысы | `club_lead` | Өз клубын басқару |
| Студент | `student` | Клубтарға қосылу, іс-шаралар |

---

## 8. Жоба күйі (әр өзгерістен кейін жаңартылады)

### Орындалды
- **2026-09-20** · Қаңқа жиналды: npm workspaces (`server` + `web`), Vite proxy `/api` → `:4400`
  (CORS қажет емес).
- **2026-09-20** · Backend: Express 5, `users` кестесі (4 рөл), `node:crypto` scrypt хэші,
  JWT httpOnly cookie-де. Эндпоинттер: `POST /api/auth/register|login|logout`,
  `GET /api/auth/me`, `GET /api/health`.
  **Рөл клиенттен қабылданбайды** — жаңа аккаунт әрқашан `student`.
  Тексерілді: `health` ✓, кукісіз `me` → 401 ✓, бүлінген JSON → 400 ✓, Vite proxy ✓.

### Фронтенд: нөлден жиналуда
- **2026-09-20** · Бірінші нұсқа **толық өшірілді** (`web/src`, `index.html`, `dist`) —
  пайдаланушының шешімі, git-ке сақталмады. Қалғаны: `package.json`, `vite.config.js`,
  `node_modules` (React 19, react-router-dom, react-icons, Vite орнатулы).
- **2026-09-20** · Нөлдік нүкте: таза ақ бет (4 файл) — `index.html`, `main.jsx`,
  `App.jsx` (`return null`), `index.css`. Dev сервер бетті береді ✓, `vite build` өтеді ✓.
  Роутер, компоненттер — әлі жоқ, қажет болған сайын қосылады.
- **2026-09-20** · Бренд түстері `index.css` ішіндегі `:root`-қа бекітілді: `--bg` +
  `--accent` және оның hover/active/soft туындылары.
- **2026-09-20** · **Логин беті** (`pages/Login.jsx` + `Login.css`): ақ фонда «жүзетін»
  карточка (380px, radius 18px, жұмсақ көлеңке + жұқа край), ішінде логотип →
  «Добро пожаловать» → «Почта» → «Пароль» (көрсету/жасыру, Ionicons көзі) →
  «Забыли пароль?» → көк «Войти». Почта onBlur-да inline тексеріледі, форма
  `POST /api/auth/login`-ге қосылған, қате орысша көрсетіледі, түймеде loading спиннері.
  Түйме мен көз түймесі pointer-down-та жауап береді (`scale`), `prefers-reduced-motion`
  өңделген. Chrome headless скриншотымен көзбен тексерілді ✓, `vite build` өтеді ✓.
- **2026-09-20** · Логотип `design/logo.png`-тен дайындалды: bounding box бойынша кесіліп
  (993×419), 480px-ке кішірейтілді, ақ фоны альфаға айналдырылды (`A = 255 − R`).
  760 KB → 42 KB.
- **2026-09-20** · Қойынды: тақырып жай «Unic» (бет аты қосылмайды).
- **2026-09-20** · Favicon (`public/favicon.png`, 256×256, 15 KB): **ақ squircle
  (radius 22.37% — iOS superellipse жуықтауы) + көк толық логотип** (`#0063FD`), ені 82%,
  бұрыштары мөлдір. HTML→Chrome screenshot арқылы жасалды (AI генерация емес: бізде
  нақты логотип бар, оны қайта сызу брендті бұзады).
  Пайдаланушының шешімі: бренд логотипі ақ фонда — қосымшаның өз көрінісімен бірдей.
  Ескерту: Chrome-ның ашық қойындысында ақ шаршының шеті көрінбейді (көк логотип
  бәрібір анық); қажет болса жұқа `rgba(0,0,0,0.08)` контур қосуға болады.
- **2026-09-20** · Логин формасы қайта қаралды: жазулар өріс ішіне көшті
  («Почта», «Пароль»), фокустағы көк ореол алынып, орнына шекара түсінің жұмсақ ауысуы
  қойылды (250ms), радиус 12px. Өріс пен түйме биіктігі бірнеше итерациядан кейін
  **48px**-ке тоқтады (58px тым ауыр болды).
- **2026-09-20** · **Тіркелу беті** (`/register`) қосылды, react-router іске қосылды.
  Қайталама болмас үшін UI ортақ компоненттерге бөлінді: `AuthCard` (карточка + логотип +
  тақырып + footer), `Field` (өріс, placeholder-label, ореолсыз фокус), `PasswordField`.
  Login мен Register бір `Auth.css`-ті бөліседі.
  Беттер арасы: логинде «Нет аккаунта? Зарегистрироваться», тіркелуде «Уже есть аккаунт? Войти».
- **2026-09-20** · БД схемасы кеңейтілді: `users`-ке **`username`** және **`phone`**
  қосылды, екеуі де `unique`. `POST /api/auth/register` бес өрісті қабылдайды
  (ФИО, никнейм, номер, почта, пароль), нөмір бір пішімге келтіріледі (`+` мен цифрлар ғана),
  никнейм lowercase сақталады. Бос еместігі **бір сұраныспен** тексеріліп, нақты қай өріс
  бос емес екені айтылады («Этот никнейм уже занят» / «Этот номер уже зарегистрирован»).
  Фронтендтегі валидация ережелері сервердікімен дәлме-дәл.
- **2026-09-20** · «Запомнить меня» галочкасы қосылды әрі **шын жұмыс істейді**:
  фронтенд `remember` жалауын жібереді, `POST /api/auth/login` соған қарай cookie-ді
  7 күндік немесе сеанстық (браузер жабылғанда өшетін) қылады. Әдепкі — қосулы.
- **2026-09-20** · Шрифт: Inter (Google Fonts, 400/500/600) — Mac-та `-apple-system` SF Pro-ға
  түседі. Токендер толықтырылды: мәтін, separator, ритм, радиус, көлеңке, `--ease-out`.
- §4 дизайн ережелері күшінде қалады — жаңа фронтенд соларға сүйеніп жазылады.

- **2026-09-20** · **БД іске қосылды**: `npm run db:setup` орындалды, `unic` базасы құрылды,
  схема қолданылды, 4 демо аккаунт отырғызылды (пароль: `password123`).
  Ұштан-ұшқа тексерілді: демо аккаунтпен кіру ✓, жаңа аккаунт тіркеу ✓,
  никнейм қайталанса «Этот никнейм уже занят» ✓, нөмір қайталанса
  «Этот номер уже зарегистрирован» ✓.

- **2026-09-20** · Екі экранға Apple қозғалысы толық орнатылды: беттер арасы —
  View Transitions API (жаңа тәуелділік жоқ), карточка ішінде кезекті шығу,
  көз иконкасы мен түйме жазуының микроауысулары. Барлығы `prefers-reduced-motion`-ды
  құрметтейді. Тексерілді: консольде қате жоқ ✓, 120ms кадрында footer әлі солғын —
  кезектілік шын жұмыс істейді ✓.

- **2026-09-20** · **Парольді қалпына келтіру** толық жасалды (`/restore`, үш қадам:
  почта → код → жаңа пароль). Бэкенд: `password_resets` кестесі (бір адреске бір жол),
  `POST /api/auth/forgot | verify-code | reset`. Код — 6 цифр, `randomInt` арқылы,
  БД-да **scrypt хэшімен** сақталады, 10 минут жарамды, 5 әрекет, минутына бір рет
  жіберіледі. Пароль ауысқан соң код өшеді.
  **Қауіпсіздік шешімі:** `forgot` аккаунт бар-жоғына қарамай әрқашан `{ok:true}`
  қайтарады — әйтпесе форма арқылы тіркелген почталарды тізіп алуға болар еді.
  `reset` кодты қайта тексереді (растау қадамын айналып өтуге болмайды).
  Ұштан-ұшқа тексерілді: қате код ✓, дұрыс код ✓, қысқа пароль ✓, пароль ауысты ✓,
  жаңа парольмен кіру ✓, кодты қайта қолдану мүмкін емес ✓, жоқ почта бірдей жауап ✓.
- **2026-09-20** · `Field` компонентіндегі ақау түзетілді: сырттан берілген `className`
  базалық `field__input` класын жойып жіберетін — енді біріктіріледі.

- **2026-09-20** · Маршрут құрылымы қайта жасалды: `/` — главный экран (қорғалған,
  әзірге бос), кіру `/login`-ге көшті. `AuthContext` қайта қосылды (сессия, `ready` күйі).
  **Тіркелгеннен кейін автоматты кіру**: сервердің `register` жауабындағы cookie-мен
  бірден `/`-ке өтеді, «аккаунт құрылды» хабары алынып тасталды.
  Тексерілді: `register` → `Set-Cookie` (HttpOnly, 7 күн) ✓, сол cookie-мен `/me` ✓,
  гость `/`-ке кірсе `/login`-ге бағытталады ✓, консоль таза ✓.
- **2026-09-20** · Никнейм уникальдығы қайта расталды: қайталанса «Этот никнейм уже занят»,
  әрі регистр маңызды емес — `AUTOTEST` пен `autotest` бір никнейм саналады ✓.

- **2026-09-20** · Хат қайта жасалды: тақырыптан код алынды, логотип CID-тіркеме ретінде
  қосылды, макет қосымшаның дизайн тілінде құрылды. Шаблон браузерде рендерленіп
  тексерілді ✓, нақты хат Gmail-ге жіберілді ✓.
- **2026-09-20** · `Home` экранына «Выйти» түймесі қойылды (жоғарғы оң жақта):
  `signOut` → cookie өшеді → маршрут қорғауы өзі `/login`-ге түсіреді.

- **2026-09-20** · `Home` бетіне **Apple үлгісіндегі header** (`components/Header.jsx`),
  айналым — iCloud панелі: **толық ені**, сол шетте логотип, оң шетте профиль иконкасы,
  биіктігі 56px. Sticky + транслюцентті қабат (`--material` + `backdrop-filter`),
  контент астынан ағып өтеді.
- **2026-09-20** · Панель түсі: алдымен бренд көгі сыналып, соңында **аққа жақын**
  `rgba(247,247,250,0.82)` бекітілді (пайдаланушының шешімі). Логотип — бренд көгінде,
  иконка — `--text-secondary`. Ашық панель бетпен қосылып кетпеуі үшін scroll edge сызығы қайтарылды.
- **2026-09-20** · Профиль мәзірі — **нативті `popover`** (өз JS-сіз): сыртқа басқанда және
  Esc-пен жабылады, top layer-де тұрады. Ішінде аты-жөні, рөлі және «Выйти».
  `transform-origin: top right` — мәзір иконкадан өсіп шығады (Apple §7: триггерге байлану).
  Анимация `@starting-style` + `allow-discrete` арқылы — таза CSS.
  Көзбен тексерілді: материал ✓, мәзір позициясы ✓

- **2026-09-21** · Header-ге **мәтіндік навигация** қосылды (логотиптен кейін, сол жақта):
  бірінші сілтеме — «Главная». `NavLink` қолданылады, белсенді күй тек түспен
  көрсетіледі (`--text-secondary` → `--text`), астынан сызық жоқ.
  Селектор — `[aria-current='page']`: `NavLink` оны өзі қояды, бөлек класс қажет емес.

- **2026-09-21** · Навигацияға **«Клубы»** қосылды (`/clubs`). Екі ішкі экран пайда болғандықта
  header қайталанбауы үшін **`AppLayout`** жасалды (Header + `Outlet`), маршруттар nested болды:
  қорғау бір жерде тексеріледі — гость бүкіл ішкі экрандардан `/login`-ге бағытталады.
  `Home.css` өшіріліп, орнына ортақ `Page.css` (`.page`, `.page__title`) қойылды.
  Тексерілді: `/clubs` → 200 ✓, консоль таза ✓, гость `/clubs`-те логин бетін көреді ✓

- **2026-09-21** · **Клубтар бетінің макеті** жасалды (`ClubCard` + `Clubs.css` торы).
  Карточка: логотип → атауы → басшысы → мүше саны → күй белгісі.
  **Логотип — әріп-плейсхолдер** (атаудың бірінші әрпі, `--accent-soft` фоны):
  файл сақтау қажет емес, әрі сурет жүктелмеген клубта да карточка толық көрінеді.
  Күй: жасыл «Активен» / сары «На рассмотрении» / сұр «Закрыт» — түсті нүкте тасиды,
  мәтін қара қалады (түсті мәтіннің контрасты нашар болар еді).
  Тор `auto-fill` + `minmax(15rem, 1fr)` — media query қажет емес, өзі бейімделеді.
  Деректер әзірге **демо массивінде** (`Clubs.jsx` ішінде) — `clubs` кестесі жасалғанда API-ға ауысады.

- **2026-09-21** · Клубтар беті жеңілдетілді: тақырып пен санақ алынып, бір демо карточка қалды.
  Тор бағанының ені — `calc((100dvh - var(--header-height)) / 2)`.
  **`aspect-ratio: 1` карточкада емес, `.club__body`-де**: фото аймағы әрқашан квадрат,
  ал header оның үстінде бөлек жолақ болып қосылады (карточка биіктігі = header + квадрат).
  Соны үшін `--header-height` токені енгізілді — панель биіктігі екі жерде екі түрлі
  жазылмайды (Header.css да сол токеннен алады).

- **2026-09-21** · Ішкі экрандардың ені header-мен теңестірілді: `.page`-тен `max-width: 64rem`
  алынды. Бұрын header толық енде, ал контент ортада болғандықтан карточка логотиппен
  тураланбай тұрды (24px қарсы 63px). Енді екеуі де `--space-6` шегінісінде.
- **2026-09-21** · Тор `auto-fill` → **`auto-fit`**: бос бағандар жиналады. `auto-fill`-де
  жалғыз карточка бірінші бағанда ілініп қалатын, сондықтан туралау жұмыс істемейтін.
  `justify-content: start` — карточкалар сол шетте.

- **2026-09-21** · Карточка iCloud виджетінің құрылымына келтірілді: ішінде **өз header
  жолағы** (`.club__header`, 52px, `--bg-secondary` фоны, астында бөлгіш) және `.club__body`.
  Жолақ әзірге **бос** — толтыру келесі қадамда. Карточкаға `overflow: hidden` берілді,
  әйтпесе жолақ дөңгелек бұрыштан шығып кетер еді.

- **2026-09-21** · Клуб карточкасына жеке `--radius-widget` токені енгізілді (логин/тіркелу
  карточкалары 18px-те қалуы үшін). Мәні 24px-тен **12px**-ке түсірілді — пайдаланушының шешімі.

- **2026-09-21** · Карточка мазмұны **толық алынды** (логотип, атауы, басшысы, мүше саны,
  күй белгісі). Қалғаны: `.club__header` (бос) + `.club__body` (бос).
  Қолданылмай қалған бәрі жойылды: CSS ережелері, көмекші функциялар, демо деректер
  массиві және `plural.js`. `Clubs.jsx` енді бір бос `<ClubCard />` ғана рендерлейді.

- **2026-09-21** · Клуб карточкасының ішкі header түсі таңдалды: төрт нұсқа қатар
  салыстырылды (#F5F5F7 / accent 10% / accent 18% / толық көк) — пайдаланушы толық көкті таңдады.

- **2026-09-21** · Клуб карточкасының header-і **толық акцент көк** (`--accent`) болды,
  мәтін түсі `--text-on-accent`. Бөлгіш сызық алынды — түстің өзі шекараны көрсетеді.
  Сайт панелі аққа жақын күйінде қалды (тек карточка header-і өзгерді).

- **2026-09-21** · Сайт панелінің түсі `#F7F7FA` → **`#EFF4FF`** (мөлдірлік 0.85).
  Төрт реңк қатар салыстырылды: бейтарап сұрлар (#F1F2F6, #E9EBF1) беттен ажыратқанымен
  палитраға бөтен, ал бренд реңкі аққа жақын күйінде қалып, карточка header-інің көгімен
  бір отбасында тұрады.

- **2026-09-21** · Клуб карточкасының header-іне **атауы** жазылды: ақ (`--text-on-accent`),
  1.375rem, semibold. Ұзын атау `text-overflow: ellipsis`-пен қысқарады — карточка ені
  тұрақты болғандықтан мәтін оны итермеуі керек.
  `ClubCard` енді `name` пропсын қабылдайды.

- **2026-09-21** · Карточка header-і Apple виджет формуласына келтірілді:
  **атауы** (ақ, 1.375rem) → **контекст жолы** («42 участника · Активен», ақ 78%).
  Екеуі де `ellipsis`-пен қысқарады, `min-width: 0` қойылды — әйтпесе ұзын атау
  карточканы керіп жіберер еді. Header биіктігі бекітілмеген, мазмұнға қарай өседі.
  `ClubCard` пропстары: `name`, `members`, `status`.
- **2026-09-21** · Карточка header-іне `···` мәзірі қосылып, сосын **толық алынып тасталды**
  (пайдаланушының шешімі) — коды да, стильдері де қалдырылмады. Онымен бірге `overflow: hidden`
  карточкаға қайта оралды, сондықтан header мен фотоға бөлек радиус жазудың қажеті қалмады.

- **2026-09-21** · Карточка денесіне **клуб фотосы** қосылды (`photo` пропсы,
  `object-fit: cover`). Мысал ретінде `design/airec_logo.png` алынды: 8112×8112, 3.2 MB —
  веб үшін 800×800 (78 KB) етіп кішірейтіліп, `web/src/assets/`-ке қойылды.
  Қара фоны әдейі сақталды: логотип таңбасы ақ, фонсыз ол ақ карточкада көрінбей қалар еді.
  Төменгі бұрыштарды `.club__body` кеседі (`overflow: hidden` + радиус) — карточканың
  өзінде `overflow` мәзір үшін алынған.

- **2026-09-21** · Фото аймағы **1:1 форматқа** келтірілді: `aspect-ratio` карточкадан
  `.club__body`-ге көшті. Енді сурет ешқашан созылмайды, ал header оның үстінде
  бөлек жолақ болып тұрады.

- **2026-09-21** · Карточкадан шекара сызығы алынды (`border`), онымен бірге соған
  сүйенген hover эффектісі де. **Салдары:** фотосы жоқ клубта дене ақ фонмен қосылып
  кетеді — тек header көрінеді. Қажет болса плейсхолдер фон не көлеңке қосылады.

- **2026-09-21** · Карточка header-і жіңішкеленді: тік шегініс `--space-4` → `--space-3`,
  биіктігі **79 → 71px** (өлшеп расталды). Мәтін өлшемдері тиілмеді — тек ауа азайды.

- **2026-09-21** · Торға **«Создать клуб»** түймесі қосылды: үзік-үзік шекара,
  ортасында плюс иконкасы мен жазуы. Өлшемі көршілес карточкамен бірдей —
  биіктік қолмен есептелмейді, торда `align-items: stretch` әдепкі күйде теңестіреді.
  Hover кезінде шекара мен мәтін акцентке ауысады, фон `--accent-soft` болады.
  Әзірге әрекеті жоқ — форма келесі қадамда.

- **2026-09-21** · **Клуб құру модалі** (`ClubFormModal`). Шешім: жаңа бет емес, **модаль** —
  тапсырма 2 өрістен тұрады әрі аяқталған соң адам сол тізімге оралады, бет ауыстыру артық.
  Нативті `<dialog>` + `showModal()` алынды: күңгірттеу, `Esc`, фокус тұзағы, top layer —
  бәрі платформадан, өз JS-іміз жоқ. Анимация `@starting-style` + `allow-discrete` арқылы.
  Құрамы: фото таңдау (карточкадағыдай **1:1**, үзік шекара, таңдалған соң превью) және
  «Название клуба» өрісі; төменде «Отмена» мен «Создать».
  Екі ақау бірден түзетілді: `margin: auto` болмаса терезе сол жоғарғы бұрышқа жабысады;
  React-тің `autoFocus`-ы мұнда жұмыс істемейді (элемент терезе ашылғанға дейін
  монтаждалған) — фокус `showModal()` соң қолмен беріледі.
  Фото әзірге тек браузерде (`URL.createObjectURL`, cleanup-пен) — серверге жүктеу
  `clubs` кестесімен бірге қосылады. Құрылған клуб бірден торға қосылады (state).

- **2026-09-21** · Карточкадағы `···` мәзірі **алынып тасталды** (түймесі де, стильдері де).
  Header құрылымы сол күйінде қалды.
- **2026-09-21** · **Клубтар БД-ға сақталады**: `clubs` кестесі (`id, name, photo_url,
  status, lead_id, created_by, created_at`) және `GET|POST /api/clubs`.
  «Заявка» бөлек кесте емес — ол `status = 'pending'` клуб.
  Клуб құруға **тек `university` мен `admin`** рөлі жібереді (`requireRole`).
  Бет ашылғанда тізім API-дан жүктеледі, модаль серверден жауап келгенше жабылмайды
  («Создаём…» күйі), қате болса өрісте көрсетіледі.
  Ұштан-ұшқа тексерілді: құру ✓, қайта сұрағанда сақталған ✓, қысқа атау → 400 ✓,
  студент рөлімен → 403 ✓.

- **2026-09-21** · «Создать клуб» түймесі **клуб жоқта да карточка өлшемінде** тұрады.
  Бұрын биіктікті тор `stretch` арқылы көршісінен алатын, сондықтан тізім бос болса
  түйме жалпайып қалатын. Енді өлшем екі токенге сүйенеді: `--club-size` (тор бағаны әрі
  фото квадраты) мен `--club-header-height`. Соның үшін карточка header-іне бекітілген
  биіктік берілді — атау мен meta бәрібір бір жолда (`nowrap` + ellipsis).

- **2026-09-21** · **Клуб фотосы сақталады**. Файл жүктеу орнына: браузер суретті
  `canvas` арқылы 400×400 квадратқа кесіп (карточкадағы `object-fit: cover` сияқты),
  JPEG 0.85 сапасында data URL жасайды да, ол қарапайым JSON-мен кетеді.
  Сондықтан `multer` немесе бөлек файл қоймасы қажет болмады — §2-дегі тәуелділік
  тізімі жабық күйінде қалды.
  Сервер тексереді: `data:image/` басталуы және 700 KB шегі; `express.json` шегі 1 MB-ке
  көтерілді (әдепкі 100 KB суретке жетпейді).
  Тексерілді: фото сақталды ✓, тізімде қайтады ✓, сыртқы сілтеме → 400 ✓.

- **2026-09-21** · Клуб карточкасына **hover-де үлкею** қосылды: `scale(1.02)`, 250ms,
  `--ease-out` (Apple қисығы). §4.3 сақталды: тек `transform` анимацияланады
  (көлеңке не өлшем емес — олар макетті қайта санатады), `prefers-reduced-motion`
  кезінде қозғалыс мүлде болмайды. Масштаб әдейі аз: 2% өсу тор аралығына (16px) сыяды,
  сондықтан карточка көршісін жаппайды.

- **2026-09-22** · **Клуб парақшасы** (`/clubs/:id`, `pages/ClubPage.jsx`). Карточка енді
  `<Link>` — басқанда парақша ашылады, `viewTransition` арқылы (§4.4 ауысуы жұмыс істейді).
  Әзірге бетте тек клуб атауы мен **артқа қайту** сілтемесі бар. Ол «Назад» емес,
  «‹ Клубы» деп аталады — §4.7 бойынша нақты атау қайда апаратынын көрсетеді.
  Бэкенд: `GET /api/clubs/:id`. Id пішімі алдын ала тексеріледі — әйтпесе Postgres
  «кривой» id-ге синтаксис қатесін беріп, 500 қайтарар еді; енді ол да 404.
  Тексерілді: бар клуб ✓, жоқ id → 404 ✓, жарамсыз id → 404 ✓.

- **2026-09-22** · Клуб парақшасы екі бағанға бөлінді (`ClubPage.css`): сол жақта мазмұн,
  оң жақта **30% енді бүйір карточка**, биіктігі экран бойынша.
  Карточка header-ге жақын тұру үшін «‹ Клубы» сілтемесі сол бағанға көшірілді
  (бұрын ол бүкіл беттің жоғарысында тұрып, карточканы төмен итеретін), ал беттің
  жоғарғы шегінісі теріс `margin`-мен өтеледі. Үстінде `--space-4` ауа қалдырылған.
  Карточка `sticky` — скроллда header астында қалады. 60rem-нен тар экранда 30% болудан
  қалып, астына түседі (тар бағанда мазмұн оқылмайды).
  Ішінде **өз header-і**: «Управление участниками», көк фон, биіктігі 48px —
  клуб карточкасының header-інен (71px) кіші, иерархия сақталады.
  Дене бөлігі `overflow-y: auto` — мүшелер тізімі ұзын болса карточка ішінде
  скроллданады, бетті созбайды.

- **2026-09-22** · Бүйір карточкаға **мүшелерді басқару панелі** (`MembersPanel`) салынды,
  макет бойынша: іздеу → «ЗАЯВКИ» → «УЧАСТНИКИ» → «Добавить участника».
  **Руководитель әрқашан тізімнің басында** — сұрыптау арқылы, деректер ретіне тәуелсіз.
  Apple логикасы сақталды: өтінім шешімдері (✓/✕) ашық тұрады — бұл рөлдің күнделікті
  жұмысы; ал сирек әрекеттер `···` мәзірінде жасырын. Іздеу мен төменгі түйме
  қозғалмайды, тек тізім скроллданады. Жол биіктігі 44px-тен кем емес.
  Деректер әзірге демо (`ClubPage.jsx` ішінде) — `club_members` кестесі керек.
  Түймелердің әрекеті әлі жоқ.

- **2026-09-22** · Мүшелер панелі `apple-design` пен `ui-ux-pro-max` бойынша қайта қаралды.
  Табылған бұзушылықтар мен түзетулер:
  · Иконка-түймелердің тию аймағы 28px болатын → **44px** (түйме 32px күйінде қалып,
    көрінбейтін `::before` аймақ қосылды — Apple тәсілі).
  · `aria-label` жоқ еді → әр түймеде **нақты** белгі («Одобрить заявку: Ерлан С.»).
  · Мүшелердегі `···` ештеңе істемейтін → «Сделать руководителем» / «Исключить из клуба».
  · Қайтарымсыз әрекет **қызыл** түспен ажыратылды.
  · Іздеу: жай жол → **толтырылған капсула** (`--fill`, pill), фокуста қоюлайды.
  · Тізімге **inset бөлгіштер** қосылды (аватардан кейін басталады, iOS үлгісі).
  **Маңызды шектеу:** тізім `overflow-y: auto` ішінде тұрғандықтан ондағы мәзір
  қалқымалы бола алмайды (қиылады) — сондықтан ол жол ішінде ашылады.
  Басшы блогында (скроллданбайды) мәзір әдеттегідей түймеден өсіп шығады.

- **2026-09-22** · Клуб парақшасының сол бағаны **бір карточкаға** жиналды (`.club-card`):
  ішінде «‹ Клубы» сілтемесі, атауы және әзірге бос мәліметтер аймағы.
  Жоғарғы шегінісі бүйір карточкамен **бірдей** — екеуі де беттің `--space-8` шегінісін
  теріс `margin`-мен өтеп, header-ден `--space-4` қашықтықта тұрады. Сан қолмен
  жазылмағандықтан, бет шегінісі өзгерсе екеуі бірге жылжиды.
  Биіктігі `min-height: calc((100dvh - --header-height) / 2)` — мазмұн көбейсе өседі.

- **2026-09-22** · Клуб парақшасында **скролл жойылды**: сол бағанда екі карточка,
  бәрі экранға сыяды. Екі себеп табылды: (1) сол карточкалар экранның дәл жартысын
  алатын, үстіңгі/аралық/астыңғы шегіністер есепке алынбаған; (2) бүйір карточка
  беттің астыңғы шегінісін шегермей, экран түбіне дейін жететін.
  Енді үшеуінің де биіктігі шегіністерді шегеріп есептеледі, төменгі шеттері бір сызықта.
  700 / 900 / 1100px биіктікте тексерілді — скролл жоқ ✓

- **2026-09-22** · Жоғарғы карточка **екіге бөлінді**: сол жақта квадрат фото,
  оң жақта атауы мен күйі («● Активен · 42 участника»).
  Фото ені бекітілмеген — `height: 100%` + `aspect-ratio: 1`, сондықтан терезе
  өзгергенде пропорция сақталады әрі карточкадан шықпайды.
  Фотосы жоқ клубта тізімдегідей әріп-плейсхолдер шығады.
  Күй атаулары мен «участник» септеуі **`src/club.js`**-ке шығарылды — бұрын ол
  `ClubCard`-та жазылған, енді екеуі бір көзден алады (§5.3 қайталама жоқ).
  Үшінші баған жасалмады: экранның жартысында ол тарылып, мәтін тасымалданар еді.

- **2026-09-22** · Жоғарғы карточкаға **көк header** қосылды (ішінде «‹ Клубы», ақ мәтін),
  ал денесі вертикаль бөлгішпен **екіге** бөлінді: фото | атауы мен күйі.
  Бөлгіш жұқа — бұл әлі бір ақпарат блогы, екі бөлек карточка емес.
  Карточкаға `overflow: hidden` берілді: header жоғарғы бұрыштарды өзі қайталайды.
  **Назар аударуға тұрарлық:** көк header стилі енді үш жерде жазылған
  (`ClubCard` 71px, `side-header` 48px, `card-header` 48px). Тағы бір жерде керек болса —
  ортақ компонентке жинау керек, әйтпесе түс өзгергенде үш файл түзетіледі.

- **2026-09-22** · Клуб бетінің жоғарғы карточкасы **ені бойынша екіге бөлінді** (`.club-top` торы,
  `1.6fr 1fr`): солда көк header + фото + атауы мен күйі, оңда — жаңа бос карточка.
  Биіктік формуласы тиілмеді — екеуі де бірдей `.club-card`, сондықтан асты да, үсті де
  тураланады, скролл пайда болмайды. Отступ енді қатардың өзінде (`.club-top`),
  карточкаларда емес. Фотоға `max-width: 50%` қойылды — карточка тарылғанда
  мәтінді ытермейді. 60rem-нен тар экранда қатар бір бағанға жиналады.
  **CSS тартібі жөнінде ақау болды:** `.club-top .club-card` мен `.club-card + .club-card`
  специфичності бірдей (0,2,0), сондықтан кейін тұрғаны жеңеді — сброс бірінші
  жазылғанда оң карточка 16px төмен түсіп, бет биіктіктен асып кетті. Сброс
  `.club-card + .club-card`-тан **кейін** қойылды. Өлшеп тексерілді (1280×700, 1600×900,
  1920×1080): скролл 0, үш карточка да бүйір панельмен бір сызықта ✓

- **2026-09-22** · Жоғарғы қатардың **оң карточкасы — қатаң квадрат** (1:1), артылған енінің
  бәрі сол карточкаға кетеді: `grid-template-columns: 1fr var(--card-height)`.
  Карточка биіктігі `--card-height` айнымалысына шығарылды (`.club-page`-те) —
  бұрын формула `.club-card`-та жазылатын, енді биіктік пен квадраттың ені бір көзден.
  Өлшеп тексерілді: 1280×700 → 242×242, 1600×900 → 342×342, 1920×1080 → 432×432,
  үшеуінде де скролл 0 ✓
  Оның үстіне бос көк header қойылды — бары сол бар `.card-header` класы,
  жаңа стиль жазылмады (түс өзгерсе бір жерден өзгереді).

- **2026-09-22** · Астыңғы карточка да **екіге бөлінді** (`.club-bottom`): солда квадрат,
  оңда — артылған ен. Жоғарғы қатардың айнасы — екі қатар бірін-бірі қайталамайды.
  Ортақ ережелер (`display: grid`, `gap`) `.club-top`-пен бірге жазылды, бөлек тұрғаны
  тек `grid-template-columns` пен `margin-top`. Өлшеп тексерілді (1280×700 / 1600×900 /
  1920×1080): скролл 0, квадраттар 242² / 342² / 432², төрт карточка да бүйір панельмен
  бір сызықта ✓
  Екі астыңғы карточкаға да бос көк header қойылды (сол бар `.card-header`) — енді төрт
  карточканың да жоғарғы жолағы бірдей (48px, `--accent`).
- **2026-09-22** · Жоғарғы қатардағы квадрат карточка аталды: **«Управление клубом»**
  («Настройки» не «Действия» емес — §4.7 бойынша нақты атау, әрі қасындағы «Управление
  участниками» панелімен жұп құрайды: бірі — клубтың өзі, екіншісі — адамдары).
  Жаңа стиль жазылмады: `.card-header__title` бар `.side-header__title` ережесіне қосылды.
- **2026-09-22** · Астыңғы қатардағы квадрат карточка — **«Чат»**. Жоспар: ішінде чаттың
  свәрнутый түрі (соңғы бірнеше хабарлама), басқанда толық чатқа кіреді.
  Әзірге тек атауы қойылды: чаттың өзі жоқ (кесте де, маршрут да) — карточканы бұл сәтте
  басылатын қылсақ, сілтеме бос бетке апарар еді.
- **2026-09-22** · Астыңғы қатардың кең карточкасы — **«События»**. Клуб бетінің төрт
  карточкасы аталып болды: сведения → Управление клубом / Чат → События,
  бүйірде — Управление участниками. Іштері әзірге бос — деректер кестелері жоқ.

- **2026-09-22** · **Клубты өңдеу** жасалды. Карточка header-інің оң шетінде «Редактировать» —
  басқанда ол **«Сохранить»** болады, сол жақтағы «‹ Клубы» — **«Отмена»** (iOS үлгісі).
  Өңдеу режимінде үш нәрсе өзгереді: **фото** (квадраттың өзі түймеге айналады, үстіне
  «Изменить фото» қабаты түседі), **атауы** және **сипаттамасы** (textarea, карточканың
  қалған биіктігін алады). Үшеуі де черновик-state-те: «Отмена» оны жай тастайды.
  Бұрын модаль ішінде жатқан `squareDataUrl` — енді `web/src/photo.js`-те, екі жер бір
  көзден алады (§5.3).
  БӘД: `clubs`-қа **`description`** бағаны қосылды. Схема бар базаның үстіне қолданылатындықтан
  (`create table if not exists` ештеңе істемейді), `schema.sql`-да кестенің соңына
  `alter table clubs add column if not exists description text;` жазылды — жаңа баған
  қосқанда осы әдіс қайталанады (миграция құралы жоқ).
  API: **`PATCH /api/clubs/:id`**, рөл сүзгісі құрумен бірдей (`university`, `admin`).
  **Маңызды семантика:** сұраныс денесінде **жоқ өріс өзгермейді**, ал `null` жіберілсе —
  өшеді. Бастапқы нұсқада өрістер толық алмасатын, сондықтан тек `name` жіберген сұраныс
  фотоны өшіріп тастайтын (тест кезінде солай болды — фото `assets/airec_logo.png`-тен
  қайта жасалып орнына қойылды).
  Ұштан-ұшқа тексерілді: тек сипаттама → фото орнында ✓, атауын ауыстыру ✓,
  `photo: null` → фото өшті ✓, бос дене → 400 ✓, студент → 403 ✓, қысқа атау → 400 ✓,
  сыртқы сілтеме фото ретінде → 400 ✓.

- **2026-09-22** · Өңдеу режимі пайдаланушының **AIREC жобасындағы «Ассистент» бетінің**
  үлгісіне келтірілді (`C:\Nurkeldi\AIRec/frontend/src/components/assistant/BusinessCard.jsx`) —
  ол бет apple-design ережелерімен жасалған, сондықтан Unic те соны қайталайды:
  1. **«Готово» — сақтаудың өзі.** Бөлек «Сохранить» түймесі бір ниетке екі түйме болар еді.
     Өңдеуден шығу = сақтау; ештеңе өзгермесе — сұраныс жіберілмейді (`isDirty` — форманы
     жазбамен **салыстыру** арқылы, жалаушамен емес: жалауша әр жолда қолмен өшірілуі керек).
  2. **«Отмена» — жазбаны қайта оқу.** Бөлек «түпнұсқа» сақталмайды.
  3. **Өрістер алмаспайды.** Мәтін ↔ input ауысуы жоқ: өріс әрқашан тұрады, өңдеу одан тек
     `readOnly`-ді алады (`disabled` емес — ол оқуға келген нәрсенің өзін өшіреді).
     Рамка өңдеуден тыс мөлдір, сондықтан макет секірмейді — тек қорап «келеді».
  4. **«Отмена» қатардың кеңеюінен шығады** (`reveal-x`: `grid-template-columns: 0fr → 1fr`
     + opacity, 200ms). Элемент разметкада қалады — әйтпесе жабылу анимациясын ойнататын
     ештеңе болмайды. Бұл — «тек transform/opacity» ережесінен саналы шегініс: мазмұнның
     еніне жету тек грид тректерімен мүмкін.
  5. Қате хабары да солай ашылады (`reveal-y`), фотодағы «Изменить фото» қабаты өңдеумен
     бірге проявляется.
  Қалғаны §4.3-ке сәйкес: басу `scale(0.97)`, 150–200ms `--ease-out`, `prefers-reduced-motion`
  глобалды ереже арқылы. Екі күй скриншотпен салыстырылды: мәтіннің орны бірдей ✓
  Шапкадағы түймелерден **ақ подложка мен фокус сақинасы алынды** (пайдаланушының
  шешімі): көк фонда ол екінші түйме сияқты көрінетін. Үстіне апарғанда да, фокуста да
  жауапты мөлдірлік тасиды (`opacity: 0.7`) — AIREC-тегі `hover:opacity-70`-пен бірдей,
  §4.5-тегі «ореол жоқ» қағидасымен де сәйкес. «‹ Клубы» сілтемесі тиілмеді.

- **2026-09-23** · **Мүшелерді басқару нақты күйге көшті** — демо деректер жойылды,
  панель толығымен API-мен жұмыс істейді.
  **БӘД:** `club_members` кестесі (`club_id, user_id, role, status, created_at`,
  PK — екі баған бірге). **Өтінім бөлек кесте емес** — ол `status = 'pending'` жол:
  бөлек кесте дәл сол өрістер мен сол байланыстарды қайталар еді.
  Клубта басшы біреу ғана болуы — **базаның ережесі**, кодтағы шақыру реті емес:
  `create unique index ... on club_members (club_id) where role = 'lead'`.
  **`clubs.lead_id` бағаны жойылды:** басшы `club_members.role`-да тұр, бір факт үшін
  екі орын ерте ме, кеш пе алшақтайды. Баған бос еді, ешқайда қолданылмаған.
  **API** (бәрі `/api/clubs/:id` астында):
  · `GET /members` — құрам. Басшы бірінші (сұраныстың сұрыптауы). **Өтінімдер тізімін
    оларды шешетіндер ғана көреді**, жауапта `canManage` жалауы бар.
  · `POST /members/request` — кез келген кірген адам өзіне өтінім қалдырады.
  · `POST /members` `{username}` — никнейм не почта бойынша қосу. Сол адамның өтінімі
    бар болса, ол бірден мүшелікке айналады (қосымша «одобрить» қадамы артық).
  · `PATCH /members/:userId` — `{status:'active'}` өтінімді бекіту, `{role:'lead'}`
    басшы қылу, `{role:'member'}` қызметтен алу.
  · `DELETE /members/:userId` — шығару әрі өтінімді қабылдамау (бір ғана жолды өшіру).
  Басшы ауысқанда **ескісі сол транзакцияда алынады** — әйтпесе жоғарыдағы уникаль
  индекс екінші жолды өткізбейді.
  Клуб карточкасындағы мүше саны енді шын: `GET /clubs` пен `GET /clubs/:id` сұрауында
  `club_members` бойынша `count` (өтінімдер саналмайды).
  **Фронтенд:** `MembersPanel` құрамды **өзі** жүктейді (`clubId` пропсы) — құрам оның
  тақырыбы, ал беттің оны ұстап тұруының қажеті жоқ. Сыртқа тек мүше саны беріледі
  (`onCountChange`), клуб атының астындағы жол содан жаңарады. Әр әрекеттен кейін
  тізім қайта оқылады: сервер — жалғыз ақиқат көзі, оптимистік жаңарту жоқ.
  Рөлі жетпейтін адам әрекет түймелерін мүлде көрмейді (сервер де 403 береді).
  «Добавить участника» түймесі енді өріс ашады (`reveal-y` — сол AIREC әдісі),
  қате өрістің астында шығады.
  **Басшының мәзірі өзгерді:** бұрынғы «Изменить руководителя» ештеңе істемейтін,
  оның орнына **«Снять с должности»** мен **«Исключить из клуба»**. Басшыны ауыстыру —
  керекті мүшенің жолындағы «Сделать руководителем» (сервер ескісін өзі алады).
  **Растау:** қызыл пункт мәзірдің ішінде тұр — оған жету үшін екінші рет басу керек,
  iOS-тағы сияқты, сондықтан бөлек модаль қосылмады.
  `reveal-x` / `reveal-y` `ClubPage.css`-тен **`index.css`-ке көшті** — оларды екі
  компонент қолданады, стиль бір жерде жатуы керек.
  Ұштан-ұшқа тексерілді (curl): бос құрам ✓, студент өтінім берді ✓, қайта бергенде 409 ✓,
  университет өтінімді көрді ✓, студент өтінімдерді көрмейді ✓, бекіту ✓, никнейммен қосу ✓,
  қайта қосу → 409 ✓, жоқ никнейм → 404 ✓, басшы тағайындау ✓, басшыны ауыстыру
  (ескісі автоматты түрде мүше болды) ✓, шығару ✓, жоқ адамды шығару → 404 ✓,
  студент басқаруға тырысса → 403 ✓. Браузерде де тексерілді: нақты аккаунтпен кіріп,
  клуб бетінің скриншоты алынды — басшы, өтінім, мүше, «2 участника» саны ✓
  **БӘД-те қалған сынама деректер:** басшы — Айгерим (демо `club_lead` аккаунты),
  бір өтінім — Студент Нуркелди. Қажет болмаса, панельден өшіріңіз.

- **2026-09-23** · Екі демо аккаунт нақты адреске көшті (пайдаланушының өтініші):
  `lead@unic.kz` → **`nurk3ldi@icloud.com`**, никнейм `nurkeldi`, ФИО «Ақжігіт Нұркелді
  Мұхаметжанұлы»; `student@unic.kz` → **`ftnurkeldi@gmail.com`**, никнейм `aruzhan`,
  ФИО «Тұрғанова Аружан Асқарқызы». Пароль сол күйінде: `password123`.
  Бұл адрестерді 20-қыркүйекте тіркелген екі сынақ аккаунт иеленіп тұрған — почта `unique`
  болғандықтан олар өшірілді (пайдаланушының шешімі; екеуінде де клуб жоқ еді).
  Жаңа базада да солай шығуы үшін `sql/setup.js` пен `README.md` жаңартылды.
  Тексерілді: екі жаңа адреспен де кіру ✓, клубтағы басшылық сақталды ✓

- **2026-09-23** · **«Управление клубом» карточкасы толтырылды** (`components/ClubControls.jsx`):
  клуб күйі және клубты жою.
  **Күй — бір ауыстырғыш, үш нұсқалы тізім емес:** жұмыс істеп тұрған клуб тоқтатылады,
  тоқтатылғаны жұмысқа қайтарылады. Қазір не болып тұрғаны жоғарғы жолда көрініп тұр,
  сондықтан түйме күйді емес, **әрекетті** атайды («Приостановить клуб» / «Возобновить работу»).
  **Жою орнында расталады** (§4.7): түйме нені жоғалтатынын жазған жолды ашады
  (`reveal-y`), қызыл «Удалить» — екінші, саналы басу. Бөлек модаль қосылмады —
  бір сұрақ үшін бүкіл бетті жабу артық. Жойылған соң `/clubs`-ке қайтады.
  Бэкенд: `PATCH /api/clubs/:id` енді `status`-ты да қабылдайды (тек `active` /
  `pending` / `suspended`), `DELETE /api/clubs/:id` қосылды. Құрам жолдары
  **каскадпен** өшеді — бұл `club_members`-тегі `on delete cascade`, кодтағы қосымша
  сұраныс емес. Рөл сүзгісі бұрынғыдай: `university`, `admin`.
  Тексерілді: тоқтату ✓, қайта қосу ✓, жалған статус → 400 ✓, студент статусты
  ауыстырса → 403 ✓, студент жойса → 403 ✓, уақытша клуб (1 мүшесі бар) жойылды әрі
  құрам жолы да кетті ✓, жоқ клубты жою → 404 ✓. Браузерде көзбен тексерілді ✓

- **2026-09-23** · «Управление клубом» карточкасы `apple-design` бойынша қайта құрастырылды
  (екі функция сол күйінде: күй ауыстырғышы мен жою).
  · Жолдар **топқа** жиналды — iOS «Настройки»-дегідей вставная группа: бір бет,
    ішінен жіңішке волоскпен бөлінген. Волосок жолдың шегінісінен кейін басталады
    (`inset: 0 0 auto var(--space-4)`) — сызық мазмұнды бөледі, карточканы емес.
  · Топқа тақырып жазылмады: карточканың көк шапкасы неге қатысты екенін айтып тұр (§16.6).
  · Күй жолы — «Состояние ··· ● Активен» (iOS value row), әрекет жолы — көк мәтін,
    жою — **бөлек топта, карточканың ең түбінде** (`margin-top: auto`), ортаға тураланған
    қызыл мәтін: қайтарымсыз әрекетке жету керек, ол келесі жолда кезігіп тұрмауы тиіс.
  · Басқанда **жолдың өзі жанады** (`--fill-hover`), масштаб емес: тізім жолдары Apple-де
    солай жауап береді. Иконкалар алынды — жүйелік тізімдерде әрекет жолы таза мәтін.
  · **Растау енді нағыз alert** (`<dialog>` + `showModal()`): §16.2 бойынша қайтарымсыз
    әрекетке ғана. Екі түйме бір қатарда, волоскпен бөлінген; әдепкі фокус —
    «Отмена»-да (промах клубқа тұрмауы керек), қызыл «Удалить» оң жақта.
    Кіру мен шығу **бір жолмен**: `scale(0.96)` + еру, `@starting-style` арқылы,
    `prefers-reduced-motion` кезінде масштаб алынады.
  · **Табылған ақау:** `--bg-secondary` токені §4.1 кестесінде жазылған, бірақ `index.css`-ке
    ешқашан қосылмаған екен — сондықтан топтың фоны мөлдір болып тұрды. Токен қосылды
    (`#f5f5f7`), енді құжаттама мен код сәйкес.
- **2026-09-23** · «Приостановить клуб» да енді сұрайды (пайдаланушының өтініші).
  Сол бір `<dialog>` қолданылады, мазмұны `asking` күйінен алынады (`'pause' | 'delete'`) —
  екінші терезе жазылмады. Жоюдың түймесі қызыл, тоқтатудікі кәдімгі көк —
  түс әрекеттің салмағын айырып тұрады.
  **«Возобновить работу» сұрамайды** — ол ештеңе алмайды. §16.2: себепсіз сұрақ
  адамды «да» деп оқымай басуға үйретеді, содан кейін шын ескерту де өтпейді.
  Терезе ені 17rem → **21rem** (272 → 336px, ~+24%). Түймелер дәл тең болуы үшін
  `grid-template-columns: repeat(2, minmax(0, 1fr))` — жай `1fr` ұзын сөзге көніп,
  өз жартысын кенейтіп жібереді («Приостановить» «Отмена»-ны қысып тұрған).
  Өлшеп расталды: 336px, түймелер тең ✓
- **2026-09-23** · Alert түймелері қайта сызылды (пайдаланушының өтініші): волоскпен
  бөлінген жазулар орнына — **әрқайсысы өз алдына боялған тақтайша**
  (radius 12px, биіктігі 44px, араларында бос орын). «Отмена» — сұр (`--fill`),
  растау — акцент көк, жою — қызыл, екеуінде де ақ мәтін. Тең ені `flex: 1 1 0`
  арқылы (базасы 0 болмаса, ұзын сөз өз жартысын кенейтеді). Жою терезесі де сондай.
  Өлшеп расталды: төрт түйме де 148px ✓

- **2026-09-23** · **Клуб чаты жасалды.**
  **БӘД:** `club_messages` (`id, club_id, author_id, body, created_at`),
  индекс `(club_id, created_at)` — чат әрқашан бір клуб бойынша, уақытпен оқылады.
  Автор `on delete set null`: адам жойылса да переписка байланысты күйде қалады,
  аты «Удалённый участник» болып шығады.
  **API:** `GET|POST /api/clubs/:id/messages`. Соңғы 50 хабарлама беріледі
  (жауапта ескісі жоғарыда — оқу реті). Кіру құқығы: **клубтың белсенді мүшесі**
  немесе клубты басқаратын рөл; басқасына 403.
  **Жаңарту тәсілі — опрос (polling), 5 секунд.** WebSocket жаңа тәуелділік әрі өз
  қосылым күйін талап етеді, §2 тізімі жабық — клуб перепискасы үшін 5 секунд
  байқалмайды. Қойынды жасырылса (`document.hidden`) опрос тоқтайды: көрінбейтін
  экран үшін серверді мазалаудың қажеті жоқ. Керек болса SSE-ге (жаңа пакетсіз)
  немесе `ws`-ке көшуге болады — сол кезде §2 бойынша сұрау керек.
  **Экрандар:** клуб бетіндегі «Чат» карточкасы — **свёрнутый вид** (соңғы 4 жол,
  ең жаңасы астыда, тек оқу); карточка түгелдей `/clubs/:id/chat`-қа апаратын сілтеме,
  hover-де тізімдегі карточкалардай 1.02 үлкейеді. Толық чат бетінде хабарламалар
  пузырьмен: өзіңдікі оңда акцент түсінде, өзгенікі солда сұр фонда, аватар мен атымен.
  Жіберілген хабарлама **бірден** тізімге қосылады (келесі опросты күтпейді).
  Жазу өрісі мен түйме 48px (§4.5), бос өрісте түйме сөнеді.
  **Ортақ көмекшілер:** `shortName` мен `initial` `MembersPanel`-ден `people.js`-ке
  шығарылды (§5.3), опрос аралығы мен уақыт пішімі — `chat.js`-те.
  Ұштан-ұшқа тексерілді: бос чат ✓, руководитель жазды ✓, мүше жауап берді ✓,
  лента реті дұрыс ✓, бос хабарлама → 400 ✓, клубқа кірмеген адам оқи алмайды → 403 ✓,
  жаза алмайды → 403 ✓, админ (басқарушы) көреді ✓. Браузерде екі экран да
  скриншотпен тексерілді ✓

- **2026-09-23** · Навигацияға **«Чаты»** қосылды (`/chats`). Сілтеме бір жерге апаруы
  керек болғандықтан, бірге **чаттар тізімі** жасалды — Apple-дің Messages тізімі сияқты:
  клуб суреті → атауы → соңғы хабарлама («Кім: не») → оң жақта уақыты.
  **Тізім «мен қайда жазысамын» деген сұраққа жауап береді**, «қайда кіре аламын» емес:
  сондықтан онда **мүше болған клубтарым** ғана тұрады. Университет/админ кез келген
  клубтың чатын клуб бетінен ашады, бірақ бұл тізімге ол түспейді.
  Сервер: **`GET /api/chats`** (`server/src/routes/chats.js`). Әр клубтың соңғы
  хабарламасы `left join lateral` арқылы бір өтуде алынады — клуб саны қанша болса да
  сұраныс біреу. Реті: соңғы сөйлескен жоғарыда (`coalesce(m.created_at, c.created_at)`),
  яғни клубқа қашан кіргенің емес, әңгіменің жаңалығы шешеді.
  Тізім де 5 секундта жаңарады — әйтпесе «қайда жауап келді» деген жерде өтірік айтар еді.
  Уақыт пішімі: бүгінгі — сағатпен, ескісі — күнімен (`chatStamp`, `chat.js`).
  Тексерілді: эндпоинт ✓, бет браузерде ✓, навигацияда белсенді күй көрінеді ✓

- **2026-09-23** · Чат экраны **екі панельге бөлінді** (WhatsApp Web үлгісі, пайдаланушының
  сілтемесі): бір карточканың ішінде солда — тізім, оңда — ашық әңгіме, аралары волоск.
  Ашық әңгіме тізімде `--accent-soft`-пен белгіленеді (`NavLink` өзі қоятын
  `aria-current='page'` арқылы) — «мен қайдамын» сұрағына жауап тұрады.
  **Бөлек `/clubs/:id/chat` беті жойылды:** бір әңгімені екі жерде көрсетудің қажеті жоқ.
  Әңгіменің өзі `components/ChatRoom.jsx`-ке шығарылды, клуб бетіндегі «Чат» карточкасы
  енді `/chats/:id`-ке апарады. Әңгіме адресте тұрады, сондықтан сілтемені жіберуге болады.
  Оң панельдің шапкасында клуб атауы — ол клуб бетіне апаратын сілтеме (әңгіме — клубтың
  бөлігі, аралдағы нәрсе емес).
  60rem-нен тар экранда панельдер қатар сыймайды: біреуі ғана көрінеді
  (`/chats` — тізім, `/chats/:id` — әңгіме), сонда ғана «‹ Чаты» түймесі шығады.
  **Ақау түзетілді:** `.chats__back { display: none }` мен `.card-header__back`-тің
  специфичності бірдей (0,1,0) болғандықтан, кең экранда артық түйме көрініп тұрды —
  селектор `.chats .chats__back` қылынды.
  Екі енде де (1600px пен 760px) скриншотпен тексерілді ✓

- **2026-09-23** · Чаттар тізіміне **іздеу** қосылды. Сол капсула мүшелер панелінде
  бар болатын, сондықтан ҧайталамай ортақ компонентке шығарылды: **`SearchField`**
  (`.side-search*` стильдері `SearchField.css`-те `.search*` болды). Екі жер де соны алады (§5.3).
  Іздеу **клуб атауы бойынша да, соңғы хабарлама мәтіні бойынша да** жүреді —
  тізімде екеуі де көрініп тұр, сол сөзді есте сақтайды. Серверге сұраныс жіберілмейді:
  тізім әлдеқашан жүктелген, сүзгі клиентте. Ештеңе табылмаса — «Ничего не нашли».
  Екі экран да скриншотпен тексерілді (мүшелер панелі де бұзылмады) ✓
  **Тазалау түймесі өзіміздікі** (пайдаланушының өтініші): Chrome `input[type=search]`-қа
  қалың көк глиф салады — ол акцентпен таласады әрі стильге көнбейді.
  `::-webkit-search-cancel-button` өшіріліп, орнына `IoCloseOutline` қойылды:
  `stroke-width: 32 → 24` (иконка штрихпен салынған, сондықтан CSS-тен жіңішкертуге көнеді),
  түсі `--text-tertiary`, hover-де қоюланады. Жазу бар кезде ғана көрінеді,
  тию аймағы `::before`-пен үлкейтілген (таңбаның өзі ұсақ қалады).

- **2026-09-23** · **Барлығына ортақ чат** қосылды (пайдаланушының өтініші): тізімнің басында
  тұрады әрі `/chats` адресінде **әдепкімен оң жақта ашылады** — бос панель мұнда не
  істейтінін айтпайтын.
  **БӘД: бөлек кесте жасалмады** — `club_messages.club_id` **nullable** болды, `null` дегені
  ортақ чат. Жаңа кесте дәл сол өрістерді бір белгі үшін қайталаған болар еді.
  **API:** `GET|POST /api/chats/general/messages` — кіргеннің бәрі оқиды да, жазады да
  (клуб мүшелігі сұралмайды — ол сондықтан ортақ). `GET /api/chats` жауабына
  `general.last` қосылды — тізімдегі жол да соңғы репликаны көрсетеді.
  `ChatRoom` екеуіне де жарайды: `clubId` болмаса — ортақ чат (айырмасы тек адресте).
  Ауысқанда `key` беріледі — лента жаңадан басталады, өзгенікіне жалғанбайды.
  Іздеу ортақ чатты сүзбейді: ол біреу және әрқашан орнында.
  Тексерілді: бос чат ✓, студент жазды ✓, ешқандай клубта жоқ админ жазды ✓,
  клуб чатымен араласпайды ✓, браузерде скриншот ✓

- **2026-09-23** · **Ортақ чат алынып тасталды** — пайдаланушыны дұрыс түсінбегенмін.
  Шын мәселе басқа болып шықты: **чат клубпен бірге пайда болуы керек**, ал тізімде
  **мүше болған** клубтар ғана тұратын — сондықтан өзі құрған клубтың чаты көрінбейтін.
  Енді `GET /api/chats` **чатты оқу құқығымен бірдей**: мүшеге — өз клубтары,
  `university`/`admin`-ге — барлығы. Жаңа клуб құрылған сәттен бастап оның чаты тізімде
  тұрады — бөлек «чат құру» деген әрекет жоқ.
  `club_messages.club_id` қайтадан `not null` (баспанасыз хабарлама болмайды),
  `/api/chats/general/messages` эндпоинттері жойылды.
  Тексерілді: клуб құрылды → чаты бірден тізімде ✓, мүше емес студент оны көрмейді ✓,
  ортақ чат адресі → 404 ✓, браузерде екі клуб тізімде ✓ (сынақ клубы сосын өшірілді).

- **2026-09-23** · Әңгіме шапкасы үлкейді (48 → **72px**) әрі толықты: солда клубтың
  аватары (фотосы не әрпі), қасында үстінде — **клуб аты**, астында — **мүшелердің аты**
  (топтық чаттағыдай). Шапканың өзі клуб бетіне апаратын сілтеме.
  Мүшелер бір жолға сыйдырылады (`ellipsis`) — аттар көбейгенде шапка өспейді.
  Кұрам `GET /api/clubs/:id/members`-тен бөлек сұралады: чаттар тізімі клуб туралы
  тек аты мен фотосын біледі.
  Жаңа биіктік **екі панельге де** берілді (`.chats .card-header`) — әйтпесе көк жолақ
  екі саты болып көрінетін. Скриншотпен тексерілді ✓
  Сосын шапкадан **навигация алынды** (пайдаланушының өтініші): енді ол сілтеме емес,
  шеврон да жоқ — тек мәлімет: қайдасың және мұнда кім бар. Клубқа «Клубы»
  бөлімі арқылы кіреді.

- **2026-09-23** · Хабарлама пузырі WhatsApp үлгісіне келтірілді (пайдаланушының фотосы):
  · **Аватар репликаның басында** (`align-items: flex-start`) — оқу жоғарыдан басталады.
  · **Автордың аты түсті**, әрі түс адамға бекітілген: `authorColor(id)` — бұл жай көркемдік
    емес, репликаны оқымай тануға көмектеседі. Түстер `--author-1..6` токендерінде,
    іші қою реңдер: жүйелік iOS түстері ашық пузырьде 4.5:1-ге жетпейді.
    **Ақау болды:** алғашқы хэш (символдар қосындысы, сосын `*31`) барлығын бір түске
    жинады — UUID-тердің пішіні бірдей, әрі 31 mod 6 = 1. **FNV-1a** қойылды
    (`Math.imul` + xor); 6000 кездейсоқ id-де таралуы тексерілді — әр түске ~1000.
  · **Уақыт соңғы жолдың соңына отырады** (`float: right`) — қысқа реплика оның кесірінен
    екінші жолды алмайды. Узын да, қысқа да хабарламамен тексерілді ✓
  · Жазу аймағы **бір ғана карточка** болды (пайдаланушының өтініші): бұрын шекаралы өріс
    мен дөңгелек түйме қатар тұратын, үстіне бөлгіш сызық — үш шекара. Енді біреу:
    толтырылған капсула (`--fill`), ішінде өріс пен жіберу түймесі. Фокусты карточканың
    өзі көрсетеді (§4.5 — сақина жоқ), бос өрісте түйме сол карточканың ішінде сөнеді.
    Сол шетіне **плюс түймесі** қойылды (тіркемеге арналған) — **әзірге әрекеті жоқ**,
    тіркеме механизмі шешілмеген (6-тармақты қараңыз).
  · Пузырь шапкасы: **солда никнейм** (`@username`, автордың түсімен), **оңда нөмірі**
    (`+7 701 000 00 03` болып топталады, `formatPhone`). Жол өзі пузырьге ең кіші ен
    береді. Өз хабарламаңда шапка жоқ — кім жазғаны жағынан көрініп тұр.
    Сервер `username` мен `phone`-ды да береді. **Ескерту:** нөмір сол чатты оқитын
    барлық адамға көрінеді (WhatsApp тобындағыдай). Керек болса — жасыруға не
    тек басшыларға көрсетуге болады.

- **2026-09-24** · Жазу карточкасындағы **плюс енді мәзір ашады** (пайдаланушының өтініші;
  әзірге **тек дизайн** — пункттердің әрекеті жоқ): «Документ», «Фото и видео», «Аудио».
  · **Жаңа мәзір стилі жазылмады.** Жобада `.row-menu` бұрыннан бар еді (мүшелер панелінде),
    сондықтан оның базалық түрі мен `row-menu-in` анимациясы `MembersPanel.css`-тен
    **`index.css`-ке көшірілді** — енді екі экран бір көзден алады (§5.3). Панельде тек
    өзіне тиесілі `.row-menu--inline` (жол ішінде ашылатын нұсқа) қалды.
  · **Мәзір триггердің өзінен өседі** (§4.3): `transform-origin: bottom left`,
    `scale(0.96)` + еру, 200ms. Жоғары ашылады — астында орын жоқ.
  · Жабылуы басқа мәзірлермен бірдей: сыртқа басқанда және Esc-пен.
  · Жол биіктігі 40 → **44px** (Apple-дің минималды тию аймағы). Бұл екі мәзірге де әсер етті.
  · **Үшінші рет CSS реті ұстап алды:** `.row-menu--up` мен базалық `.row-menu`-дің
    специфичності бірдей (0,1,0), ал сборкада компонент файлы `index.css`-тен бұрын тұрды —
    сондықтан мәзір төмен, әрі 16px енімен шықты. Селектор `.chat__attach-box .row-menu--up`
    қылынды: енді файл реті маңызды емес.
  Ашық күйі нақты құрастырылған CSS-пен скриншотта тексерілді ✓
  · **Иконкалар қалыңдатылды** (пайдаланушының өтініші): `stroke-width` 32 → **44**,
    мөлдірлігі 0.7 → 0.85 — 18px өлшемде әдепкі штрих semibold мәтіннің қасында солғын.
    **Астын сызып қоюға тұрарлық:** қалыңдық `<svg>`-де емес, **фигураның өзінде**
    атрибут болып тұрады (react-icons солай рендерлейді), сондықтан `svg`-ге жазылған
    ереже оны жеңе алмайды — селектор `.row-menu__item svg *` болуы керек.
    Өлшеп расталды: `getComputedStyle(path).strokeWidth` = 44px (бұрын 32px).
    Екі мәзірге де қатысты.

- **2026-09-24** · Хабарламаға **өз мәзірі** қосылды (пайдаланушының өтініші): курсор
  апарғанда пузырьдің оң жоғарғы бұрышында шеврон шығады, басқанда мәзір түседі.
  · **Түйменің орны** (пайдаланушының фотосы бойынша түзетілді): өзгенің репликасында ол
    **шапка жолының соңында** тұрады (нөмірден кейін), өз репликаңда шапка жоқ —
    сондықтан **пузырьдің сыртында, сол жағында** (ішінде оған уақыт кедергі жасайды).
    Тыныш күйде түймеге **бір пиксель де орын бөлінбейді**: ол `reveal-x` полосасында
    отырады (`grid-template-columns: 0fr → 1fr`) — курсор келгенде **нөмір солға жылжиды**,
    түйме босаған орыннан шығады (пайдаланушының өтініші). Зазор `gap`-пен емес,
    шегініспен берілген — әйтпесе жиналған полоса нөмірден кейін бос жер қалдырар еді.
    Пузырьдің ені өзгермейді. Hover жоқ құрылғыда (`@media (hover: none)`) полоса әрқашан ашық.
  · Мәзір **түйменің астынан түседі** (§4.3 — триггерге байлану), түрі ортақ `.row-menu`.
  · Пункттер **шын жұмыс істейді**, бос емес:
    **«Копировать»** — `navigator.clipboard`, жаңа тәуелділік жоқ. Жобада тост жүйесі
    болмағандықтан, растауды мәзірдің өзі көрсетеді: жазуы 900ms «Скопировано» болып
    тұрады да, мәзір жабылады.
    **«Удалить»** — қызыл, тек өз хабарламаңда (немесе `university`/`admin` рөлінде).
  · Бэкенд: **`DELETE /api/clubs/:id/messages/:messageId`** — шарт бір сұраныста:
    `($3 or author_id = $4)`, сондықтан «кімдікі» деп бөлек оқудың қажеті жоқ.
    Өңдеу жасалмады: түзетілген реплика өзгенің есінде оқылып қойған, ал «изменено»
    белгісі — бөлек әңгіме.
    Тексерілді: бөтен адам өшіре алмайды → 404 ✓, авторы өшірді ✓, қайта өшірсе → 404 ✓.
  Ашық мәзір нақты құрастырылған CSS-пен скриншотта тексерілді ✓

- **2026-09-24** · **Жауап беру (reply)** қосылды — пайдаланушының фотосындағыдай.
  **БӘД:** `club_messages`-ке `reply_to uuid references club_messages(id) on delete set null`.
  Цитаталанған хабарлама өшсе, жауаптың өзі жойылмайды — тек цитатасы жоғалады.
  **API:** `POST /messages` енді `replyTo`-ны қабылдайды. Сілтеме **сол клубтың** хабарламасына
  ғана байланады: `insert ... select ... left join club_messages r on r.id = $4 and r.club_id = $1`
  — бөтен id жіберілсе, қате берілмейді, жай байланыспайды (`reply_to = null`).
  Лента цитатаны **сол сұраныспен** алады (`left join club_messages r` + автордың `users`-і),
  сондықтан қосымша сұрау жоқ.
  **Интерфейс:**
  · Мәзірге «Ответить» қосылды — басқанда жазу өрісі фокусқа келеді.
  · Цитата жолағы **жазу өрісімен бір карточканың ішінде** (пайдаланушының фотосы
    бойынша): үстінде цитата (солда түсті жолақ, оңда ✕), астында волоскпен
    бөлінген өріс. `reveal-y` арқылы ашылады, кіру мен шығу бір жолмен.
    Карточка осы сәтте капсуладан дөңгелек төртбұрышқа айналады
    (`:has(.reveal-y--open)`) — екі жол сыйғанда капсула пішіні жарамайды.
    **Ақау болды:** ол карточкаға `overflow: hidden` қойылғанда тіркеме мәзірі
    қиылып қалды — ол жоғары ашылады, сондықтан плюс түймесі «істемейтіндей» көрінді.
    Шешім: `overflow` алынып, цитата жолына өз радиусы берілді
    (`border-radius: --radius-card --radius-card 0 0`).
  · Пузырьдегі рет: **ник пен нөмір — ең үстінде**, одан кейін цитата, сосын жауап
    мәтіні (пайдаланушының фотосы бойынша): алдымен кім сөйлеп тұр, сосын неге жауап береді.
  · Цитата **пузырьдің ішінде**: сол жақта түсті жолақ, астында ник
    пен бір жол мәтін (`ellipsis`). Түс автордың түсі — `currentColor` арқылы жолаққа да,
    никке де бірдей беріледі.
  · **Цитатаны басқанда түпнұсқаға өтеді** (`scrollIntoView`, `block: 'center'`) — әңгіме
    жібін жоғалтпайды. Ол үшін әр хабарламаға `id="msg-<id>"` берілді.
  Тексерілді: жауап сақталды әрі лентада цитатасымен қайтты ✓, жоқ id → байланыспады ✓,
  екі бағыт (өз/өзге хабарлама) скриншотпен ✓

- **2026-09-24** · Чат лентасына **өрнекті фон** қойылды (пайдаланушының фотосы — WhatsApp):
  `--bg-secondary` төсеніш + ұсақ фигуралардан тұратын солғын өрнек. Өрнек — **ішкі SVG**
  (`data:` URL, 180×180 тайл): жаңа файл да, тәуелділік те қажет емес.
  **§5.4-тен саналы шегініс:** өрнектің түсі суреттің ішінде жазылған (`%23000` + 3.2% альфа) —
  CSS айнымалылары `data:` URL ішіне кірмейді. Альфа қарадан алынғандықтан,
  кез келген ашық төсеніште дұрыс көрінеді.
  Фон өзгергендіктен пузырь де түзетілді: өзгенің хабарламасы енді `--fill` емес,
  **тығыз `--surface`** — мөлдір фоннан өрнек көрініп, мәтін ластанатын еді.
  «Здесь пока пусто» деген жол да капсулаға отырды — өрнектің үстінде жалаңаш тұрмайды.

- **2026-09-24** · Әңгіме шапкасын басқанда оң жақтан **үшінші панель** ашылады — «Данные клуба»
  (WhatsApp-тағы «Данные группы» сияқты). Әзірге **іші бос**, шапкасында ✕ бар.
  Панель адреске түспейді: бұл — бүйірден қарау, өтетін жер емес.
  Жабық күйде баған ені `0` (әрі `display: none`), ашықта `22rem` — екеуі де нақты сан,
  сондықтан `grid-template-columns` жумсақ интерполяцияланады (`0fr`-дан `minmax()`-ке ауысу
  анимацияланбайтын еді).
  60rem-нен тар экранда үшеуі сыймайды: панель ашылса, әңгіменің орнын алады.
  Шапка енді түйме (`aria-expanded`), үстіне апарғанда жеңіл мөлдірлікпен жауап береді.

- **2026-09-25** · Навигацияға **«События»** қосылды (`/events`). Сілтеме бір жерге апаруы
  керек болғандықтан, іс-шаралардың өзі жасалды.
  **БӘД:** `club_events` (`id, club_id, title, place, starts_at, created_by, created_at`),
  индекс `starts_at` бойынша — тізім әрқашан уақытпен оқылады.
  Сипаттама бағаны әдейі жоқ: атауы, уақыты, орны — іс-шара туралы тізімде керегі осы,
  ұзын мәтін керек болса кейін қосылады.
  **API:**
  · `GET /api/events` — **тек болашақтағылар** (`starts_at >= now()`), клуб атауы мен
    фотосымен бірге. Көріну ережесі чаттағымен бірдей: мүшеге — өз клубтары,
    `university`/`admin`-ге — барлығы.
  · `GET /api/clubs/:id/events` — бір клубтың іс-шаралары (өткені де көрінеді: ол — тарих).
    Кірген адамның бәріне ашық, клуб беті де солай.
  · `POST /api/clubs/:id/events` — тек `university`/`admin`. Күн `new Date(...)`-пен
    тексеріледі: жарамсыз болса `Number.isNaN(getTime())` ұстайды.
  · `DELETE /api/clubs/:id/events/:eventId`.
  **Экрандар:**
  · `/events` — жақын іс-шаралар тізімі: солда **күн плашкасы** (сан + ай), ортасында
    атауы мен «уақыт · орын», оң жақта клуб аты. Жол — сол клубқа апаратын сілтеме.
  · Клуб бетіндегі «События» карточкасы толтырылды (`components/ClubEvents.jsx`):
    сол тізім ықшам түрде, басқарушыға — жолдағы қоқыс себеті және астында
    «Добавить событие» (форма `reveal-y`-мен ашылады: атауы, күні-уақыты, орны).
    Күн енгізу — нативті `datetime-local`, жаңа тәуелділік жоқ.
  · Карточканың астындағы `.side-add` түймесі енді **екі жерде** (мүшелер мен іс-шаралар),
    сондықтан оның стилі `MembersPanel.css`-тен `index.css`-ке көшірілді (§5.3).
  Ұштан-ұшқа тексерілді: құру ✓, күнсіз → 400 ✓, студент құрса → 403 ✓, клуб тізімі ✓,
  жалпы тізім (мүше көреді) ✓. Екі экран да браузерде скриншотпен ✓
  (Сынама іс-шара «Ярмарка клубов» 2026-09-27-де өшірілді.)

- **2026-09-26** · Клуб бетіндегі **«Чат» карточкасы қайта жасалды** (пайдаланушы «дұрыстау
  керек» деді, үш нұсқа ұсынылып, ол **«бір соңғы хабарлама»** нұсқасын таңдады).
  Бұрын: төрт жол қатар, астына жабысқан — карточканың жартысы бос тұратын, үш бірдей
  сұр жол ештеңені бөліп көрсетпейтін, әрі ол баратын жерімен (пузырьлер, аватар, өрнек)
  бір тілде сөйлемейтін.
  Енді Apple-дің Messages виджетіндегідей: **аватар + аты** (чаттағы сол түспен),
  астында **соңғы реплика** (1rem, 4 жолмен қиылады — `-webkit-line-clamp`),
  уақыты, ал ең түбінде «Открыть чат ›». Карточка бұрынғыдай толығымен сілтеме.
  **Ортаға қатысты сабақ:** `cat > file` алдымен файлды **тазартады**, ал Vite watcher
  дәл сол сәтте оқып, **бос модульді кэштеп** қалады — беті ақ болып қалады
  (`#root` бос, модульдің `sourcesContent` — `""`). Түзету: файлды қайта сақтау
  (мазмұнды оқып, сол күйінде жазу) — watcher қайта оқиды. Қайта қосудың қажеті жоқ.

- **2026-09-26** · **Чатқа фото жіберу** жасалды. Плюс мәзіріндегі «Фото и видео» енді жүйелік
  файл таңдау терезесін ашады (`<input type="file" accept="image/*">`, өз тереземіз жоқ).
  **Сақтау тәсілі — клуб фотосындағыдай data URL** (6-тармақтағы ашық сұрақ осылай шешілді):
  жаңа тәуелділік те, файл қоймасы да қажет болмады, §2 жабық күйінде қалды.
  · **Браузер сығады**, бірақ клуб фотосынан айырмасы — **кеспейді**: пропорция сақталады,
    ұзын қыры ≤ 1280px, JPEG 0.82; ауыр болса 0.7 → 0.55-ке түседі (`chatPhoto`, `photo.js`).
    Суретті жүктеу коды екі функцияға ортақ `loadImage`-ке шығарылды.
  · **БӘД:** `club_messages`-ке `photo` (data URL), `photo_width`, `photo_height`.
    Жазусыз фотода `body` — бос жол (`not null` сақталды).
  · **Маңызды шешім — лентада сурет жоқ, тек адресі бар.** Опрос 5 секунд сайын соңғы 50
    хабарламаны қайта алады: суреттердің байттары ішінде жүрсе, әр опрос мегабайттар болар еді.
    Сондықтан лента `photo: { url, width, height }` береді, ал суреттің өзі —
    **`GET /api/clubs/:id/messages/:messageId/photo`** (құқығы лентамен бірдей, `canReadChat`).
    Хабарлама өңделмейтіндіктен, сурет сол адресте ешқашан өзгермейді —
    `Cache-Control: private, max-age=31536000, immutable`: браузер оны бір рет қана жүктейді.
  · Сервер тексереді: `data:image/(jpeg|png|webp);base64,…` пішімі, 900 000 таңба шегі
    (`express.json`-ның 1 MB шегінен төмен — қасында жазу да бар), өлшемдері 1…4096.
    Хабарлама бос болмауы керек: не мәтін, не фото.
  · **Лента секірмейді:** өлшемдер алдын ала белгілі, сурет орны `aspect-ratio`-мен
    жүктелгенге дейін ұсталады (жүктелгенше `--fill` төсеніші тұрады). Ені 16rem,
    биіктігі 20rem-ден аспайды — өте биік сурет ортасынан кесіледі, басқанда жаңа қойындыда
    толық ашылады.
  · Жазусыз фотода уақыт **суреттің үстіне** түседі (`--scrim` плашкасында) — әйтпесе оған
    бос жол бөлу керек еді.
  · Жіберер алдында сурет **жазу карточкасының ішінде** күтеді (цитата жолағындай, `reveal-y`):
    кішкентай превью, «Фото», ✕. Өріс плейсхолдері «Подпись» болады, түйме бос жазумен де
    белсенді. Жабылу анимациясы кезінде превью жоғалып кетпеуі үшін соңғы сурет `ref`-те ұсталады.
  · Мәтінсіз хабарлама басқа жерлерде **«Фото»** деп аталады: цитатада, жауап жолағында,
    чаттар тізімінде, клуб бетіндегі «Чат» карточкасында. Мәтіні жоқ хабарламаның мәзірінде
    «Копировать» шықпайды. `GET /api/chats` енді соңғы хабарламаны мәтінмен емес,
    уақытымен анықтайды (бұрын жазусыз фото «хабарлама жоқ» болып көрінер еді).
  Ұштан-ұшқа тексерілді: жазусыз фото ✓, жазуымен әрі фотоға жауап ✓, сурет байттары
  дәл қайтады ✓, `Content-Type` ✓, кэш тақырыбы ✓, лентада base64 жоқ ✓, тізімде «Фото» ✓,
  бос → 400 ✓, сыртқы сілтеме → 400 ✓, сурет емес → 400 ✓, жарамсыз өлшем → 400 ✓,
  тым үлкен → 400 ✓, жоқ сурет → 404 ✓. Лента мен превью скриншотпен тексерілді ✓
  (сынама хабарламалар кейін өшірілді).
  **Видео әлі жоқ:** пункт «Фото и видео» деп аталады, бірақ тек сурет қабылдайды —
  видео data URL-ға сыймайды, оған файл қоймасы керек.

- **2026-09-26** · Чат лентасының фоны **пайдаланушының өз суретіне** ауысты
  (`design/unic_chat_background.png`; сол күні пайдаланушы жаңа нұсқасын берді — 2294×1290, тек сұр иконкалар) — бұрынғы ішкі SVG өрнегі алынды.
  · Жобаға **WebP lossless** болып кірді (`web/src/assets/chat-background.webp`):
    PNG-ден әлдеқайда жеңіл, пиксельдері бірдей. Vite оны хэшпен шығарады — браузер кэштейді.
  · Сурет тайл емес, біртұтас сурет, сондықтан `center / cover` — плиткамен қайталанбайды.
    Лента скроллданғанда фон орнында тұрады (тұсқағаз сияқты).
  · **Суреттің фоны таза ақ**, ал өзгенің пузырі де ақ (`--surface`) — олар бірігіп кетер еді.
    Шешім: `background-blend-mode: multiply` + `--bg-secondary` астары — ақ жері `#F5F5F7`
    болады, иконкалар өзгермейді. Суреттің өзін қайта боямау керек болды, әрі түс токеннен.
  Скриншотпен тексерілді ✓

- **2026-09-26** · Чаттағы фото үш жерде түзетілді (пайдаланушының өтініші):
  · **Уақыт тек курсор апарғанда** көрінеді (жазусыз фотода — суреттің үстіндегі плашка).
    Тыныш күйде кадр ештеңемен жабылмайды. `opacity` арқылы жұмсақ шығады; hover жоқ
    құрылғыда (`@media (hover: none)`) әрқашан көрінеді — әйтпесе оны білудің жолы жоқ.
  · **Сыртқы рамка жіңішкерді:** бұрын сурет пузырьдің кәдімгі шегіністерінде тұратын
    (8×12px — қалың көк жақтау). Енді фотосы бар пузырьде **3px жиек** (`--inset`),
    ал шапка, цитата мен жазу өз шегіністерін `calc(... - var(--inset))` арқылы қайтарады.
    Суреттің радиусы — пузырь радиусы минус жиек (18 − 3), бұрыштары параллель жүреді.
    Пузырь ені = фото ені + жиек, сондықтан жазу пузырьді жанына созбай, фото бойынша
    тасымалданады.
  · **Басқанда сол бетте терезе ашылады** (бұрын жаңа қойындыда): нативті `<dialog>` +
    `showModal()` — Esc, фокус, top layer браузерден. Фоны **тұтас қара** (`--viewer-bg`) —
    артқы бет мүлде көрінбейді; оң жоғарғы бұрышта ✕ (44px). Суреттің сыртын басу да жабады.
    Кіру мен шығу бір жолмен: фон ериді, сурет 0.96-дан өседі (`@starting-style` +
    `allow-discrete`). Жабылу кезінде сурет жоғалып кетпеуі үшін соңғысы `ref`-те ұсталады.
    Жаңа токендер: `--viewer-bg`, `--viewer-fg`.
    ✕ түймесі **подложкасыз** — жай ақ таңба (пайдаланушының үлгісі бойынша): қара фонда
    оған плашка керек емес. Иконка 36px (штрихы оның жартысынан азын алады), тию аймағы 44px, hover-де `opacity: 0.7`.
    Сосын **жіңішкерді** (пайдаланушының өтініші): `IoClose` → **`IoCloseOutline`**, `stroke-width` **24**
    (іздеудегі тазалау ✕-імен бірдей). **Маңызды:** `IoClose` — **толтырылған** фигура, оның қалыңдығы
    геометрияда, `stroke-width` оған **әсер етпейді**; жіңішкерту үшін тек `*Outline` иконка жарайды.
    Осы себепті «Данные клуба»-дағы ✕ бұрын «жіңішкерді» деп жазылғанымен, шынында өзгермеген еді —
    ол да `IoCloseOutline` + 24 болды (`.chats__info-close`), екі ✕ енді шын мәнінде бірдей.
    **Ескерту:** жоба тек ашық тақырыпта, бірақ фото қарау терезесі қара — сурет қара
    фонда жақсы көрінеді (iOS Photos пен WhatsApp-та да солай). Керек болса ақ фонға ауыстыруға болады.
  Тірі серверде скриншотпен тексерілді: жіңішке жиек ✓, уақыт жасырын ✓, терезе ашылды ✓

- **2026-09-26** · **Өз хабарламаңның мәзірі** шевронның қасынан ашылатын болды (пайдаланушының
  скриншоты бойынша). Бұрын мәзір әрқашан пузырьдің оң жоғарғы бұрышына байланған еді —
  өзгенің репликасында шеврон дәл сонда (шапкада), ал өз репликаңда шеврон **пузырьдің
  сыртында, сол жақта** тұрады, сондықтан мәзір түймеден алыс, фотоның үстіне шығып тұратын.
  Енді мәзір JSX-те `menu` айнымалысына шығарылды: өзгенікінде — бұрынғыдай пузырьдің ішінде,
  өзіңдікінде — шевронмен бірге `.msg__more-box`-та (`position: relative`). Түйменің астынан
  түседі де, **солға**, лентаның бос жағына қарай ашылады (`right: 0`, `transform-origin: top right`) —
  пузырь мен фото жабылмайды. Екі аккаунтта скриншотпен тексерілді (фото мен мәтін) ✓

- **2026-09-26** · **Чат экраны толық ашылады** — карточка емес, шеттерге тақалған (пайдаланушының
  шешімі). Себебі: чат — адам ұзақ отыратын жұмыс орны, оқитын бет емес; сайт панелі беттің
  шекарасын көрсетіп тұр, ал оның астындағы карточка жиегі «рамканың ішіндегі рамка» болатын.
  Messages / Telegram / Slack / WhatsApp Web те солай.
  · `.chats`-тан теріс `margin`, жиек пен радиус алынды, биіктігі — `100dvh - --header-height`.
  · Беттің шегіністерін алу үшін **`.page--flush`** модификаторы (`Page.css`) — басқа беттер
    бұрынғыдай карточкада қалады. Селектор құрама (`.page.page--flush`): бір салмақта
    тұрса, `.page`-тің media query ішіндегі шегінісі ережелер ретіне қарай жеңіп кетер еді.
  · Панельдерді бұрынғыдай волоск бөледі.
  · `overflow: hidden` **қалуы керек** (алғашында алынып, ақау берді): «Данные клуба» панелі
    0-ден ашылғанда оның 1px жиегі шетінен шығып, бетке айналдыру жолақтарын қосатын.
  1500px пен 760px енде скриншотпен тексерілді ✓

- **2026-09-26** · **Ақау түзетілді: лента өзінен-өзі төмен секіретін.** Опрос әр 5 секунд сайын
  жаңа массив береді, ал түбіне айналдыру эффекті `[messages]`-ке байланған еді — жоғарыда
  тарих оқып отырған адамды әр опрос төмен лақтыратын.
  Енді екі шарт бар: (1) эффект **соңғы хабарламаның id-сіне** байланған (`lastId`) —
  өзгеріссіз опрос ештеңе қозғамайды; (2) айналдыру тек адам **түбінде тұрса** болады —
  `onScroll` соны `atBottom` ref-інде ұстайды (48px қор: «түбіне жақын» да түбі саналады).
  Өз хабарламаңды жібергенде лента әрқашан соған түседі (`atBottom = true` жіберер алдында).
  Браузерде өлшеп тексерілді (20 сынама хабарлама, кейін өшірілді): ашқанда түбінде ✓,
  жоғарыда оқып отырғанда 2 опрос өтті — орнында қалды ✓, сол кезде жаңа хабарлама келді —
  орнында қалды ✓, түбінде тұрғанда жаңа хабарлама келді — соған түсті ✓

- **2026-09-26** · Чат шапкалары **көктен ақ тулбарға** ауысты (пайдаланушы көк шапка ыңғайсыз,
  «Чаты» бөлігі бос деді). Толық экранда көк шапка 1920px-лік қою тақта болып,
  хабарламалармен назар үшін таласатын; сайттың ашық панелімен қатар тұрған екінші,
  бөтен шапка сияқты көрінетін. Үлгі — Mac-тағы Messages / Telegram.
  · Үш панельдің шапкасы да `--surface`, астында волоск, биіктігі бірдей (4rem) —
    сызық бір түзу болып тартылады. Барлығы `.chats .card-header*` құрама селекторларымен
    (базалық стиль `ClubPage.css`-те, клуб бетінің карточкалары көк күйінде қалды).
  · Сол жақта «Чаты» атауы алынды — ол навигацияда тұр; атауы скринридер үшін
    `visually-hidden` `<h1>`-де қалды, ал шапканың орнын **іздеу** алды (`.search` шапкаға кірді).
  · Әңгіме шапкасы: аты — `--text`, мүшелері — `--text-secondary`, әріп-аватар — тізімдегідей
    `--accent-soft`. «‹ Чаты» — акцент түсінде (iOS back сияқты), ✕ — сұр.
  · Шапкалар `justify-content: flex-start` — тар экрандағы ақау кетті: клуб аты мен аватар
    бұрын оң шетке ығысып тұратын, енді «‹ Чаты»-дан кейін тұрады.
  · Көк түс тек мағынасы бар жерде қалды: өз хабарламаң, ашық чат, жіберу түймесі.
  · Тізімнің үстіңгі 8px шегінісі алынды — бірінші чат шапканың волоскіне тақалады.
  Үш күйде скриншотпен тексерілді (кең экран, «Данные клуба» ашық, 760px) ✓

- **2026-09-26** · «Данные клуба» панелі толықты (пайдаланушының WhatsApp скриншоты бойынша):
  аватардың астында **клуб аты** (1.5rem, semibold, ортада) және **«Клуб · 2 участника»**
  (сан акцент түсінде — WhatsApp-та жасыл, бізде бренд көгі). Сан — чат шапкасы үшін
  бұрыннан жүктелетін құрамнан (`members.length`, тек белсенді мүшелер), септеуі
  `membersLabel` (`club.js`) — жаңа сұраныс та, жаңа көмекші де жоқ.
  Панельдегі ✕ **жіңішкерді**: `stroke-width: 44` алынды, фото терезесіндегі ✕-пен бірдей
  (иконканың әдепкі штрихы; өрісі алдымен 2rem, сосын пайдаланушының өтінішімен **1.5rem** — таңба кішірейді, тию аймағы түйменің өзінде 44px қалды). Скриншотпен тексерілді ✓

- **2026-09-26** · «Данные клуба» панеліне **«Медиа, ссылки и документы»** жолы қосылды
  (WhatsApp скриншоты бойынша; **тек интерфейс** — басқанда әзірге ештеңе болмайды).
  iOS «Настройки» тобы: иконка (`IoImagesOutline`) → атауы → шеврон, биіктігі 3.25rem,
  hover/басқанда жолдың өзі жанады. Скриншоттағы санақ (2) **қойылмады** — ойдан сан
  жазғанша бос қалғаны дұрыс; оның орнында шеврон тұр. Санақ бөлімнің өзімен бірге келеді.
  · **Жаңа стиль жазылмады:** «Управление клубом»-дағы `.group` / `.group__row` қайта
    қолданылды. Енді оны екі жер пайдаланатындықтан, базасы (топ, жол, волоск, label)
    `ClubControls.css`-тен **`index.css`-ке көшірілді** (§5.3); картаға тән нұсқалары орнында қалды.
  · **Төртінші рет CSS реті ұстап алды:** көшіргеннен кейін «Удалить клуб» ортадан сол жаққа
    ығысты — `.group__row--danger` пен базалық `.group__row` бірдей салмақта, әр түрлі файлда.
    Селектор `.group__row.group__row--danger` қылынды. **Ереже:** базалық стильді
    `index.css`-ке көшіргенде, оның модификаторлары бар ма — сол сәтте тексеру керек.
  Екі экран да скриншотпен тексерілді (чат панелі, клуб беті) ✓

- **2026-09-26** · «Данные клуба» тобына **«Уведомления»** жолы қосылды — iOS ауыстырғышымен
  (**тек интерфейс**: күйі `useState`-те, сақталмайды, хабарландырудың өзі әлі жоқ).
  · Ауыстырғыш — **нативті `<input type="checkbox" role="switch">`**, `appearance: none`
    арқылы iOS пішінінде (51×31, тұтқасы 27px): пернетақта, скринридер, `label` — бәрі
    өзінен жұмыс істейді, өз JS-іміз жоқ. Бүкіл жол `<label>` — тұтқаны көздемей, жолдың
    кез келген жерін басуға болады.
  · Тұтқа `transform`-пен жүреді, жол тек түсін ауыстырады (§4.3). Қосулы түсі — **бренд көгі**
    (iOS-те жасыл, бірақ Apple ауыстырғышты акцентпен бояуға рұқсат береді).
  · Жаңа токендер: `--switch-off` (iOS-тің сөндірулі жолы) және `--shadow-knob`.
  · Екі жол бір топта, волоскпен бөлінген; ортақ класы `.chats__media` → **`.chats__row`** болды.
  Екі күй скриншотпен тексерілді ✓

- **2026-09-26** · Чаттағы төрт кемшілік жабылды (пайдаланушының тізімі бойынша):
  1. ~~**Баған ені**~~ (46rem, ортада) — **сол күні қайтарылды**: пайдаланушының 1920px
     экранында әр жақта ~370px бос қалды. Төмендегі жазбаны қараңыз.
  2. **Күн бөлгіштері** — «Сегодня» / «Вчера» / «24 сентября» (өзге жылдікі жылымен), `dayLabel`
     (`chat.js`). Күн жергілікті уақытпен салыстырылады (`toDateString`), UTC-мен емес.
     Капсула **`position: sticky`** — айналдырғанда жоғарыда тұрып, қай күн екенін көрсетеді.
     Тығыз фон — астынан пузырьлер өтеді. (Алғашқы нұсқадағы ақау — төмендегі жазбада.)
  3. **Көп жолды өріс.** `input` → `textarea` (`rows=1`): мазмұнға қарай өседі (8 жолға дейін,
     сосын айналады), биіктікті `useLayoutEffect` береді — бір кадр ескі биіктікте тұрмайды.
     **Enter — жіберу, Shift+Enter — жаңа жол**; IME теріп жатқанда Enter соған тиесілі
     (`isComposing`). Бір жолдан асқанда капсула карточкаға айналады (`.chat__box--tall`),
     түймелер астыңғы жолға жабысады (`align-items: flex-end`).
  4. **«Медиа, ссылки и документы» мен «Уведомления» енді шын жұмыс істейді.**
     · **Медиа:** `GET /api/clubs/:id/media` — фотолар (адрес пен өлшем, байт емес) және
       хабарламалардан жиналған сілтемелер. Жолда санақ тұр; басқанда сол панельдің ішінде
       келесі экран ашылады (iOS «Настройки» сияқты, ‹ артқа): фото торы (3 баған, шаршы,
       басқанда толық ашылады) және сілтемелер тобы (адрес, кім, қашан). **Документ бөлімі жоқ** —
       оларды әлі жіберу мүмкін емес, бос бөлім көрсетпедім; экран атауы «Медиа и ссылки».
     · Сілтемелер лентада да **басылатын** болды (`withLinks`, жаңа қойындыда ашылады).
       Шаблон біреу: `LINK_RE` (`chat.js`) серверде де сол — не жарықталса, сол жиналады.
       Сөйлем соңындағы нүкте-үтір адреске кірмейді (тексерілді).
     · **Уведомления:** браузердің жүйелік хабарландыруы (`Notification` API, жаңа тәуелділік жоқ).
       **БӘД:** `chat_mutes (user_id, club_id)` — **өшірілгендер** сақталады, қосулысы — әдепкі,
       сондықтан жаңа клубқа ештеңе жазудың қажеті жоқ. `PUT /api/clubs/:id/notifications
       {enabled}`, ал `GET /api/chats` енді `muted` пен соңғы хабарламаның `id`/`authorId`-ын береді.
       Ауыстырғыш бірден жауап береді (сервер артынан), қате болса қайтады. Браузер рұқсатын
       **қосқан сәтте** сұрайды — адамның қимылына жауап ретінде. Астында iOS-тағыдай ескерту
       жолы: не болатынын не неге болмайтынын айтады (рұқсат жоқ / браузер қолдамайды).
     · Хабарландыруды **`ChatNotifier`** жібереді — ол `AppLayout`-та, сондықтан чат бетінде
       болмасаң да (басқа бет, басқа қойынды) жұмыс істейді. Лентадан айырмасы — жасырын
       қойындыда да сұрайды (хабарландырудың мәні сонда). Үндемейді: өз хабарламаңда, өшірулі
       чатта, көз алдыңда ашық тұрған чатта. Рұқсат жоқ кезде серверге мүлде бармайды.
       `tag` — бір чаттың жаңа хабары ескісін ауыстырады, үйілмейді; басқанда сол чат ашылады.
       **Шектеу:** қойынды жабық болса келмейді (ол үшін push керек — «Келесі қадам» 6).
     · Фото терезесі **`PhotoViewer`** компонентіне шығарылды (оны енді чат та, «Медиа» да
       қолданады), стилі `PhotoViewer.css`-ке көшті. `.side-title` екі жерде керек болғандықтан
       `MembersPanel.css`-тен `index.css`-ке көшті (модификаторы `.lead-block .side-title` құрама — қауіпсіз).
  **Тексерілді:** API — медиа ✓, сілтеменің соңғы нүктесі кесіледі ✓, өшіру/қосу ✓, қайта өшіру
  идемпотентті ✓, біреудің баптауы өзгеге әсер етпейді ✓, boolean емес → 400 ✓, жоқ клуб → 404 ✓.
  Браузерде (жүйелік `Notification` жалғанмен ауыстырылып): басқа бетте тұрғанда хабарландыру
  келді, мәтіні «KBTU У.: …» ✓, өшірулі чатта келмеді ✓, ашық чатта келмеді ✓. Фото терезесі
  ашылып-жабылады ✓, Enter жіберді ✓, Shift+Enter жібермеді ✓. Бес экран скриншотпен ✓.
  Барлық сынама хабарламалар өшірілді.

- **2026-09-26** · Чат бағанын шектеу **алынды** (пайдаланушы: «екі жақ бос қалды»). Пузырьлер
  мен жазу өрісі қайтадан панельдің бүкіл енінде, WhatsApp Web-тегідей; `--chat-column`
  токені мен `max(…, (100% − 46rem) / 2)` шегіністері өшірілді — бұрынғы шегіністер қайтты.
  **Сабақ:** «ыңғайсыздықтың себебі — кең баған» деген менің болжамым еді, пайдаланушы
  растамаған; жасамас бұрын скриншотпен екі нұсқаны көрсеткен дұрыс еді.
  Сосын пайдаланушы «кішкене жақындат, әдемі көрінетін қылып» деді — толық ен де, тар баған да
  емес. 1920px экранда **56 / 64 / 72rem** қатар салыстырылды, **`--chat-column: 64rem`** таңдалды:
  әр жақта ~270px (46rem-де ~415px еді), пузырьлер шетке шашырамайды, жазу өрісі сол бағанда.
  Шегіністер формуласы қайтты: `max(space, (100% − var(--chat-column)) / 2)`. Сан бір жерде —
  басқа ен керек болса, тек токенді өзгерту жеткілікті.
  **Күн бөлгішінің ақауы түзетілді:** жабысқан капсулалардың бәрі бір тізімнің ішінде тұрғандықтан
  жоғарыда **қабаттасып жиналатын** — «Сегодня» қысқа болғандықтан, астындағы «24 сентября»
  шетінен көрініп тұрды (пайдаланушының скриншоты). Енді әр күн — өз `<section class="chat__day-group">`-ы:
  жабысқан элементтің шекарасы — ата-анасы, сондықтан капсула тек өз күні айналып жатқанда
  тұрады, ал келесі күн оны жоғары **итеріп шығарады** (Telegram-дағыдай). Лента `days` тізіміне
  топталады (`sameDay`), `Fragment` керек болмай қалды.
  Өлшеп тексерілді (20 сынама хабарлама, кейін өшірілді): шекарада ескі капсула −9..18px,
  жаңасы 30px — қабаттаспайды ✓; жаңа күннің ішінде тек біреуі жоғарыда ✓. 1920px скриншот ✓

- **2026-09-26** · Жазу өрісінің астындағы **ақ жолақ алынды** (пайдаланушының өтініші): тұсқағаз
  енді экранның түбіне дейін жетеді, өріс переписканың үстінде **қалқып** тұрады (Telegram / WhatsApp).
  · Тұсқағаз `.chat__list`-тен жаңа орауыш **`.chat`**-қа көшті (`ChatRoom` енді `<>` емес,
    `<div className="chat">` қайтарады) — лента да, өріс те бір фонның үстінде.
  · Өріс фоны `--fill` (жартылай мөлдір) → **`--surface` + `--shadow-card`**: мөлдірдің астынан
    өрнек көрініп тұратын, ал көлеңке өрісті переписадан бөледі. Фокуста сұр түске ауысу алынды —
    ақ картада ол бөтен көрінер еді; фокусты курсор көрсетеді.
  Скриншотпен тексерілді ✓

- **2026-09-26** · «Данные клуба» панеліне **мүшелер бөлімі** қосылды (WhatsApp-тағы «Данные группы»
  скриншоты бойынша): тақырып жолы «2 участника» + оң жақта лупа, астында **барлық мүше**:
  әріп-аватар (лентадағы автор дөңгелегімен бірдей) → аты (өзің — **«Вы»**, бірінші тұрасың) →
  басшыда астында көк **«Руководитель»** белгісі → оң жақта `@никнейм`.
  · Лупа іздеу өрісін ашады (`reveal-y`, бар `SearchField` — жаңа стиль жоқ), фокус бірден соған
    түседі; жиналған өріс `inert` — Tab оған түспейді. Іздеу аты мен никнейм бойынша (`@` белгісін
    елемейді), сервер сұралмайды. Чат ауысса іздеу тазарады.
  · Сервер: `GET /api/clubs/:id/members` жауабына **`username`** қосылды (`publicMember`) —
    мүшелер панелі оны елемейді, ештеңе бұзылмады.
  · «Новые сообщения придут системным уведомлением…» ескерту жолы **алынды** (пайдаланушының
    өтініші), «Уведомления этого чата выключены» де — ауыстырғыштың өзі айтып тұр. Жол енді тек
    ауыстырғыш уәде бергенін орындай алмағанда шығады: браузер рұқсат бермесе не қолдамаса.
  Екі күй скриншотпен тексерілді (тізім, «nurk» бойынша іздеу) ✓

- **2026-09-26** · «Данные клуба» панелі **кеңейді**: 22rem → **26rem** (352 → 416px, пайдаланушының
  өтініші) — ішінде никнеймдері бар мүшелер тізімі мен фото торы бар. Тізім панелі өзгерген жоқ.

- **2026-09-27** · **Ақау түзетілді: соңғы хабарламаның мәзірі қиылып қалатын** (пайдаланушының
  скриншоты). Мәзір әрқашан шевронның астына түсетін, ал лентаның түбінде оған орын жоқ —
  `overflow-y: auto` оны жазу өрісінің үстінде кесіп тастайтын.
  Енді мәзір ашылар сәтте **кнопканың астында қанша лента қалғанын өлшейді**: `11rem`-ден
  (үш жол × 44px + шегіністер) аз болса — **жоғары ашылады** (`.row-menu--above`,
  `transform-origin: bottom right` — iOS-тегідей кнопкадан өседі). Шек rem-де, px-ке
  түбірдің шрифт өлшемімен аударылады — пайдаланушы шрифтті үлкейтсе де дұрыс.
  Екі жағдай да бар: өз хабарламаң (мәзір шевронмен бірге пузырьдің сыртында) және
  өзгенікі (шеврон шапкада — мәзір пузырьдің үстіне шығады). Селекторлар құрама.
  Екі аккаунтта өлшеп тексерілді: соңғы хабарлама — жоғары, лентаға толық сыйды ✓. Скриншот ✓

- **2026-09-27** · Мүшелер тізіміндегі жол **екі қабат** болды (пайдаланушының өтініші — толық ФИО
  сыймай, кесіліп тұрды): үстінде **толық аты-жөні** (0.875rem, кесілмейді — сыймаса тасымалданады),
  астында **`@никнейм`**, басшыда сол жолда « · Руководитель» (көк). Оң жақтағы никнейм алынды —
  ол аттан ен жейтін.

- **2026-09-27** · **Профиль беті** (`/profile`, `pages/Profile.jsx`) — header-дегі профиль иконкасы
  енді popover мәзір ашпайды, **бетке апарады** (`NavLink`, `viewTransition`; ашық тұрғанда
  иконка навигациядағыдай қоюланады — `aria-current`).
  · Apple ID беті сияқты, ортада тар бағанда (36rem): **үлкен әріп-аватар** (чат пен құрамдағы
    дөңгелекпен бірдей) → **толық аты** → `@никнейм`; астында iOS тобы (`.group`, `<dl>`):
    Почта · Номер (`formatPhone`) · Роль · «В Unic с» (`createdAt`); ең түбінде бөлек топта
    қызыл **«Выйти»**. Шығу сұрамайды — ештеңе жоғалмайды, қайта кіруге болады (§4.7).
  · Рөл атаулары (`ROLE_LABELS`) `Header.jsx`-тен **`people.js`-ке** көшті. Header-дегі мәзірдің
    коды мен стильдері (`.header__menu*`, popover) **толық алынды** — енді қолданылмайды.
  · Деректер сессиядан (`useAuth().user`, `GET /api/auth/me`) — жаңа сұраныс жоқ.
  Тексерілді: иконка → `/profile` ✓, белсенді күй ✓, «Выйти» → `/login` ✓, шыққан соң `/me` → 401 ✓.
  Скриншот ✓
  **Келесі қадам (профиль):** не қосылатыны пайдаланушымен шешіледі — мысалы, өңдеу
  (аты, никнейм, нөмір), құпиясөзді ауыстыру, менің клубтарым, аватар суреті.

- **2026-09-27** · Университет профилі жасалып (екі баған: визитка + 4 көрсеткіш, «Клубы университета»,
  «Ближайшие события», `GET /api/clubs/stats`), сол күні пайдаланушының өтінішімен **бос бетке
  айналды** — университет профилі қайтадан жиналады. Қолданылмай қалған бәрі өшірілді:
  шолу компоненті, оның стильдері, `api.clubStats`, серверлік `/stats`.
  Қазір: `university` пен `admin` үшін `/profile` — бос `<main className="page">`,
  студенттің профилі (визитка) өзгерген жоқ.
  **Ескерту:** «Выйти» тек профиль бетінде болатын — бос бетте университет аккаунтының
  шығатын жолы жоқ. Профиль қайта жиналғанда ол міндетті түрде қайтуы керек.

- **2026-09-27** · Университет профилінің бірінші қадамы: **сол жақ жоғарғы бұрышта 1:1 логотип**
  (алдымен 10rem, сосын пайдаланушының өтінішімен **300×300** — `18.75rem`; әрпі 7.5rem, `--radius-card`). Сурет жоқта — аты-жөнінің бірінші әрпі (`--accent-soft`).
  Басқанда жүйелік файл таңдау ашылады; курсор апарғанда астынан «Добавить фото» /
  «Изменить фото» жолағы шығады (hover жоқ құрылғыда әрқашан көрінеді); жүктелгенше квадрат
  солғындап, басылмайды.
  · **БӘД:** `users.photo` (data URL, клуб фотосындағыдай). Суретті браузер 400×400-ге кесіп
    сығады — бар `squareDataUrl` (`photo.js`) қайта қолданылды.
  · **Сессия суретті алып жүрмейді:** middleware бұрынғыдай жеңіл бағандарды ғана оқиды
    (сурет ~50–100 KB, ол әр сұранысқа қосылар еді). Суретті `GET /api/auth/me` (қосымша
    ашылғанда бір рет) бөлек дочитывает, кіру жауабы (`select *`) өзі береді.
  · **API:** `PUT /api/auth/photo {photo}` — data URL қояды, `null` өшіреді. Тексерісі клуб
    фотосымен бірдей (`data:image/`, 700 000 таңба). Жауабындағы `user`-мен `setUser` —
    header пен бет бірден жаңарады.
  Тексерілді (API): әдепкі `null` ✓, қою ✓, `/me` қайтарады ✓, қайта кіргенде орнында ✓,
  сілтеме → 400 ✓, тым үлкен → 400 ✓, өзгеде сурет жоқ ✓, өшіру ✓. Браузерде: әріп ✓,
  файл таңдалды → квадратта сурет, серверде JPEG ✓ (сынама сурет кейін өшірілді).

- **2026-09-27** · Header навигациясы **дәл ортаға** қойылды (пайдаланушының өтініші). Бұрын ол
  логотиптің қасында, сол жақта тұратын (`.header__left`). Енді `.header__inner` — үш бағанды тор
  `1fr auto 1fr`: солда логотип, ортада навигация, оң жақта профиль (`justify-self: end`).
  Шеткі бағандар тең болғандықтан, логотип профиль иконкасынан кең болса да, орта ығыспайды.
  `.header__left` ораушысы мен оның стильдері алынды.
  Өлшеп тексерілді: 1500px — header ортасы 743, навигация 742; 760px — 373 / 372; айналдыру жоқ ✓

- **2026-09-27** · Header навигациясы **үлкейді әрі анық болды** (пайдаланушының өтініші):
  әріп 15 → **17px** (Apple навигациясының өлшемі), аралары 20 → **32px** (`--space-8`).
  Қарапайым сілтеме `--text-secondary` (сұр) → **`--text`** (анық қара); **ашық беттің сілтемесі —
  `--accent`**, курсор апарғанда да көкке ауысады (қайда баратынын сол түспен ескертеді).
  Телефонда (< 34rem) бұрынғыдай ықшам: 15px, аралары 16px — әйтпесе 400px-те навигация
  сыймай, бетке көлденең айналдыру қосылатын (өлшеп табылды).

- **2026-09-27** · Header-дегі профиль түймесі **аккаунттың өз суретін** көрсетеді (дөңгелек,
  `object-fit: cover`), сурет жоқта — бұрынғы `IoPersonCircleOutline`. Дерегі сессиядағы
  `user.photo` — сурет жүктелген сәтте (`setUser`) header бірден жаңарады. Профиль ашық
  тұрғанда көк сақина қосылып еді — пайдаланушының өтінішімен **алынды**: тек фото, рамкасыз.

- **2026-09-27** · Профильдегі 300×300 суретке **жіңішке шекара** қосылды (`1px solid --separator`,
  пайдаланушының өтініші) — ақ фонды логотип ақ беттен ажырап тұрады.

- **2026-09-27** · Университет логотипі **карточкаға салынды** (пайдаланушының өтініші): ені беттің
  алдымен 50%-ы, сосын **70%**-ы, беттің **ортасында** (`margin-inline: auto`), биіктігі header астындағы экранның **50%**-ы (`(100dvh − --header-height) / 2`).
  Стилі клуб бетінің карточкаларымен бірдей: `--surface`, `1px --separator`, `--radius-widget`,
  header-ге сол `margin-top` айласымен тартылған. Логотип ішінде сол жақ жоғарғы бұрышта,
  300×300, сосын **400×400** (пайдаланушымен келісіп: 1920×1080-де карточканың ішкі биіктігі ~470px,
  300px астында бос жер қалдыратын; әрі сурет дәл 400×400 сақталады — экранда пиксель-пиксельге) —
  бірақ **карточкадан биік болмайды** (`height: min(25rem, 100%)` + `aspect-ratio: 1`, әрпі 10rem):
  аласа экранда квадрат кішірейеді, шетінен шықпайды. 60rem-нен тар экранда карточка толық енде.

- **2026-09-27** · Логотиптегі «Изменить фото» жолағының орнына **екі түйме** (пайдаланушының
  өтініші): қызыл **«Удалить»** және көк **«Изменить»** — әрқайсысы өз тақтайшасында (радиус 12px,
  биіктігі 44px, тең ені `flex: 1 1 0`, аралары 8px), иконкасыз. Суреттің астында, курсор
  апарғанда немесе фокуста шығады (hover жоқ құрылғыда — әрқашан). Сурет жоқта — тек «Добавить».
  · Квадрат енді **түйме емес**, `div`: түйменің ішіне түйме салуға болмайды (HTML).
    Бұрынғы «бүкіл квадратты басу» әрекеті жоқ — файл таңдау «Изменить»/«Добавить» арқылы.
  · «Удалить» — `PUT /api/auth/photo {photo: null}`, растаусыз: сурет қайта екі басумен қойылады.

- **2026-09-27** · Университет карточкасында логотиптің **оң жағына атауы мен никнейм** шықты
  (клуб бетінің жоғарғы карточкасындағыдай: солда фото, оңда кім). Атауы 2rem semibold
  (ұзын болса тасымалданады), астында `@никнейм`, одан кейін **почта** мен **телефон нөмірі** (`formatPhone`; бәрі `--text-secondary`,
  бір блок); логотиптің жоғарғы
  жиегімен тураланған. Карточка — `flex`; логотип өз бағанында (`.profile__photo-box`,
  карточканың бүкіл биіктігі — квадраттың `min(25rem, 100%)` шегі содан есептеледі).

- **2026-09-27** · Университет карточкасында байланыс деректерінің астына **екі цифр-тақтайша**
  (пайдаланушының өтініші): **«Клубы»** — барлық клуб саны, **«Участники»** — белсенді мүшелер
  (қолтаңба атау септігінде: «Клубов» болса «1 клубов» деп қате оқылатын),
  **қайталаусыз** (бір адам екі клубта болса — бір). Виджет стилі: `--bg-secondary`, 12px радиус,
  сан 2rem semibold (`tabular-nums`), астында қолтаңба; тең, 10rem-ден.
  Сервер: **`GET /api/clubs/stats`** қайта қосылды — тек осы екі сан (`clubs`, `members`), бір сұраныс,
  тек `university`/`admin`, `/:id`-ден **жоғары** тұр (әйтпесе «stats» id болып кетер еді).
  Цифр келгенше тақтайшалар салынбайды — сызықшалы бос рамка нөл болып оқылар еді.

- **2026-09-27** · Цифр-тақтайшаларда **сан мен сөз бір жолда** («**1** клуб», «**2** участника»,
  `baseline` бойынша тураланған; пайдаланушының өтініші). Сөз енді санмен **септеледі** — ол үшін
  `club.js`-ке **`plural(count, [one, few, many])`** шығарылды (11–14 — әрқашан «көп» формасы);
  `membersLabel` соны қолданады, нәтижесі өзгерген жоқ. Тақтайшалар ені — мазмұнына қарай, бірақ
  екеуі тең (`width: max-content` торда `1fr`, ең кемі 10rem) — «12 участников» шетінен шықпайды.

- **2026-09-27** · Университет карточкасы **экранның бүкіл биіктігіне** созылды (бұрын жартысы):
  `100dvh − --header-height − 2 × --space-4`. Үсті де, асты да экран шетінен бірдей 16px —
  беттің 32px шегіністері екі жақтан да теріс `margin`-мен өтеледі, сондықтан айналдыру жоқ.
  Логотип бұрынғыдай 400×400-ден үлкеймейді (`min(25rem, 100%)`).

- **2026-09-27** · Университет карточкасының мәтіні **сведения торына** көшті (пайдаланушының
  үлгісі — «подпись үстінде, мән астында», үш баған): бұрынғы сұр жолдар мен екі цифр-тақтайша
  бір тілде сөйлемейтін. Логотип орнында (сол жақ жоғарғы бұрыш, 400×400) — оң жақта атауы
  (2.5rem) және `<dl>` торы **3×3**: Никнейм · Почта · Телефон / Роль · В Unic с · Клубы /
  Участники · События впереди · На рассмотрении. Қолтаңба 0.9375rem сұр, мән 1.375rem semibold
  (ұзын почта тасымалданады). 9 ұяшық торды дәл толтыруы үшін `GET /api/clubs/stats` екі санмен
  толықты: `events` (алдағы іс-шаралар), `pending` (бекітуді күтетін клубтар).
  Сандар келгенше — «—» (нөл емес, «әлі жоқ» болып оқылады). `Stats` тақтайшалары мен
  `.profile-stats*` стильдері өшірілді; `plural` (`club.js`) `membersLabel`-де қала береді.

- **2026-09-27** · Сведения торы **Apple ережесіне** келтірілді (пайдаланушы: «текст грубый»).
  Мән 22px semibold → **17px regular** (Apple тізімдеріндегі негізгі мәтін өлшемі), иерархияны
  **салмақ емес, түс** береді: қолтаңба 13px `--text-secondary` (+0.01em, §4.2), мән `--text`.
  Қолтаңба мәнге жақын (4px), жұптар арасы 24px — жұп болып оқылады. Атауы 2.5 → **2rem**.
  **Сөз ортасынан бөлінбейді:** `overflow-wrap: anywhere` почтаны «unic / .kz», нөмірді «00 / 02»
  деп бөлетін (пайдаланушының экранында — масштабы үлкен) → мән **бір жолда**, сыймаса «…»,
  толығы `title`-де. Сандар `tabular-nums`.

- **2026-09-27** · Логотип **кішірейді** (пайдаланушының ұсынысы; әрпі 6rem): мәтін жеңілдегеннен
  кейін оң жақтағы сведения блогы ~240px болды, ал 400px логотип басым тұратын. Алдымен 15rem
  қойылды — өлшегенде үсті 8px, асты 13px сәйкес келмеді (сведенияның биіктігі қаріпке тәуелді).
  **Шешім — байлау:** логотип пен сведения бір қатарда (`.profile-card__head`, `flex`).
  Алғашқы нұсқа (логотип сведенияның биіктігін алады, `height: 100%`) **ені 2px болып қалды** —
  квадраттың ені биіктігінен шығады, ал ол ені есептелген кезде әлі белгісіз (айналма тәуелділік).
  Сондықтан бағыт керісінше: логотип **16rem (256px)**, сведения `stretch`-пен соның биіктігіне
  созылып, `justify-content: space-between` — атауы логотиптің жоғарғы, соңғы қатары астыңғы
  жиегінде. Екеуі де rem-де — шрифт үлкейсе бірге өседі.

- **2026-09-27** · Сведения торы **2 бағанға** ауысты (бір жолда екі дерек), үш дерек **алынды**
  (пайдаланушының өтініші): Участники, События впереди, На рассмотрении. Қалғаны 3×2:
  Никнейм · Почта / Телефон · Роль / В Unic с · Клубы. `GET /api/clubs/stats` тек `clubs`
  санын қайтарады — қолданылмайтын есептеулер серверден де өшірілді.

- **2026-09-27** · Сведения қатарларының арасына **жіңішке сызық** (`1px --separator`, «Контакты»-дағыдай;
  пайдаланушының өтініші). Тордың бағанаралық саңылауы алынды — сызық екі бағанды бойлай
  үзіліссіз өтеді, шегіністі ұяшықтың өзі береді. Бірінші қатардың үсті мен соңғы қатардың
  асты шегініссіз — тор бұрынғыдай логотиптің жиектерімен тураланады.

- **2026-09-27** · Сведения торына **тік сызық** қосылды, әрі сызықтар **бір-біріне тимейді**
  (пайдаланушының өтініші). Бағанаралық саңылау қайтты (`--column-gap: --space-8`): көлденең
  сызық енді әр ұяшықтың өз `border-top`-ы — саңылауда үзіледі; тік сызық — сол ұяшықтың
  `::after`-і, саңылаудың ортасында, биіктігі тек ұяшықтың мәтіні бойынша (`top/bottom` =
  ұяшықтың шегіністері `--pad-top/--pad-bottom`), сондықтан көлденеңге 12px жетпейді.
  Шеткі қатарларда шегініс 0 — логотиппен туралану сақталды.

- **2026-09-27** · Университет профиліне **өңдеу** қосылды — тек **аты мен никнейм** (пайдаланушының
  өтініші), клуб бетіндегі AIRec үлгісімен: карточканың оң жақ жоғарғы бұрышында
  «Редактировать» → **«Готово»**, қасынан **«Отмена»** шығады (`reveal-x`). Мәтінді кнопкалар
  бренд көгімен (iOS навигациясындағыдай), «Готово» — semibold; тию аймағы 44px.
  · **Өрістер әрдайым орнында** (`readOnly` ауысады): тыныш күйде жиегі мөлдір, өңдеуде ақ фон
    мен `--separator-strong` жиегі проступает («поля пайда болсын»), фокуста — акцент жиек (§4.5).
    Өріс қорабы мәтіннен **сыртқа** ашылады (теріс margin = ішкі шегініс) — мәтін қолтаңбамен
    және логотиппен тураланған күйінде қалады. Никнеймде «@» өрістің сыртында тұр.
    Ұяшықтың `overflow: hidden`-і (көп нүкте үшін) өріс жиегін кесетін —
    `.profile-facts__value:has(.profile-edit) { overflow: visible }`.
  · «Готово» — сақтаудың өзі; ештеңе өзгермесе сұраныс кетпейді (`isDirty` — жазбамен
    салыстыру). Enter — «Готово», Esc — «Отмена». Қате болса өңдеу ашық қалады, мәтін сақталады.
    Сақталған `user` `setUser`-ге кетеді — header мен бет бірден жаңарады.
  · Сервер: **`PATCH /api/auth/me {fullName?, username?}`** — жоқ өріс өзгермейді; аты 2–120
    таңба (бос орындар сығылады), никнейм тіркелудегідей (`USERNAME_RE`, кіші әріппен),
    бос емесі → 409 «Этот никнейм уже занят» (жарыс болса — базаның `unique`-і, `23505` → 409).
    Жауап `photo`-мен бірге — header-дегі аватар жоғалмайды.
  Тексерілді: API (өзгерту, бос орын, кіші әріп, фото орнында, бөтен ник → 409, қате ник → 400,
  қысқа аты → 400, бос дене → 400, өз нигі → 200) ✓; браузерде: өңдеуге кіру, фокус атауда ✓,
  «Отмена» қайтарды ✓, «Готово» сақтады ✓, бос емес ник — қате, өңдеу ашық ✓. Деректер қайтарылды.

- **2026-09-27** · Университет атауы **қалыпты салмақпен** (400; 2rem өлшемі жеткілікті, semibold
  дөрекі көрінетін). Өңдеу енді **тек атауды** өзгертеді — никнейм қайтадан жай мәтін
  (`@kbtu`); никнейм өрісінің коды мен стильдері (`.profile-edit--nick`, `.profile-edit__input`,
  ұяшықтың `overflow: visible` ережесі) өшірілді. Пайдаланушының жоспары: **никнейм, почта және
  құпиясөзді өзгерту** — карточканың астында бөлек бөлім болады. Сервердегі `PATCH /api/auth/me`
  `username`-ді қабылдай береді — ник өзгерту сол арқылы жасалады.

- **2026-09-27** · Логотип пен сведенияның астына **ортақ көлденең сызық** (`.profile-card__head`-тің
  `border-bottom`-ы, `--separator`, үстінен 20px) — карточканың ішкі енін толық алады, «кім»
  бөлігін астындағы (келешектегі ник/почта/пароль) бөлімнен ажыратады.

- **2026-09-27** · Университет профиліне **«Вход и безопасность»** бөлімі (`components/AccountSecurity.jsx`):
  ортақ сызықтың астында iOS тобы — **Никнейм / Почта / Пароль**, оң жақта қазіргі мәні
  (пароль — `••••••••`), шеврон. Жолды басқанда **терезе** ашылады (нативті `<dialog>`, бір терезе,
  мазмұны ауысады; әр ашылғанда форма таза — ашылу санауышы `key`, жабылу кезінде емес).
  · **Никнейм:** бір өріс, жазып отырғанда тексеріледі (рұқсат етілмеген таңба — бірден қате),
    ештеңе өзгермесе сұраныс кетпейді; `PATCH /api/auth/me {username}`.
  · **Пароль:** қазіргі + жаңа + қайталау. Сәйкестік пен ұзындық өрістен шыққанда тексеріледі
    (§4.7), серверден келген қате **керекті өрістің астында** шығады. Сәтті болса — «Пароль
    изменён» және «Готово». `POST /api/auth/password {current, next}`: қазіргісі дұрыс болуы
    шарт (ашық компьютерден аккаунтты ұрлауға болмайды), жаңасы ≥ 8 және ескісінен басқа.
  · **Почта — екі қадам:** жаңа адрес + қазіргі пароль → жаңа адреске **6 цифрлы код** →
    код енгізілгенде почта ауысады. **БӘД:** `email_changes` (адамға бір жол, код scrypt хэшімен,
    10 минут, 5 әрекет, минутына бір рет — қалпына келтірудегідей). `POST /api/auth/email`,
    `POST /api/auth/email/confirm` (жарыс болса — `unique` → 409). Хат — **сол макет**:
    `mailer.js`-тегі `layout(code, text)` енді мәтінді параметр ретінде алады, `sendEmailCode`
    қосылды (тақырыпта код жоқ — «Код для подтверждения почты»); қалпына келтіру хаты өзгермеді.
  · Терезе стилі (`.modal*`) `ClubFormModal.css`-тен **`index.css`-ке** көшті — оны енді екі
    компонент қолданады; модификаторлары түгел бірге көшті (басқа файлда қалған жоқ).
  **Тексерілді:** API — пароль: қате қазіргі → 400, қысқа → 400, сол → 400, ауыстыру → ескісі
  кірмейді, жаңасы кіреді ✓; почта: қисық → 400, өзінікі → 400, қате пароль → 400, бос емес → 409,
  өтінімсіз код → 400, қате код → 400, дұрыс код → почта ауысты, фото орнында, код бір реттік ✓.
  Браузерде: үш жол ✓, никнейм терезесі (фокус, тікелей қате, сақтау) ✓, пароль терезесі
  (сәйкес емес, қате қазіргі — өз өрісінде, сәтті) ✓, почта терезесі қателері ✓, «Отмена» ✓,
  клуб құру терезесі бұзылмады ✓. **Хат жіберілетін қадам нақты SMTP-мен тексерілмеді** —
  бөтен адреске хат кетпеуі үшін код базаға тікелей жазылып тексерілді. Деректер қайтарылды
  (почта `university@unic.kz`, пароль `password123`, никнейм `qairu_university`).

- **2026-09-27** · Профильдегі «Вход и безопасность» бөлімі **сведения торына біріктірілді**
  (пайдаланушы «ыңғайсыз» деді: никнейм мен почта екі рет тұратын, бір карточкада екі түрлі
  дизайн, тар топ сол жаққа ығысып тұратын). Енді Apple ID-дегідей — өзгертуге болатын мәннің
  **оң жағында көк «Изменить»** (тыныш сілтеме, `.profile-facts__edit`): Никнейм · Почта / Телефон ·
  **Пароль** (`••••••••`) / В Unic с · Клубы. **«Роль» алынды** (университеттің профилі — рөл
  айтпаса да көрінеді; пайдаланушымен келісілді), тор 3×2 болып қалды, логотиппен туралау сақталды.
  `AccountSecurity` енді тізім емес — тек **терезе** (`open`, `session`, `onClose` пропстары),
  оны `UniversityInfo` басқарады; топ жолдарының стильдері өшірілді.
  **Табылған ақау:** терезені жауып, бірден басқа «Изменить» басқанда жаңа терезе жабылып
  қалатын — алдыңғы жабылудың `close` оқиғасы **кешігіп**, жаңа `showModal()`-дан кейін жетіп,
  күйді тазалайтын. Енді `onClose` тек терезе шынымен жабық болса қабылданады.

- **2026-09-27** · **Телефонды өзгерту** қосылды: сведения торындағы «Телефон» ұяшығында да
  «Изменить» → терезе (`PhoneForm`): бір өріс, қазіргі нөмір пішімделіп тұрады, өрістен шыққанда
  тексеріледі. Сервер: `PATCH /api/auth/me` енді `phone`-ды да қабылдайды — тіркелудегідей
  `normalizePhone` + `PHONE_RE`, бос емес → 409 «Этот номер уже зарегистрирован» (жарыста
  `unique` бұзылса — `constraint` атынан ник пе, нөмір ме анықталады). **SMS кодымен растау жоқ**:
  жіберетін қызмет жоқ (жаңа ақылы тәуелділік), әрі нөмір кіру кілті емес, тек байланыс.
  Тексерілді: API (жақша/бос орын → бір пішім, бөтен → 409, қысқа → 400) ✓, браузерде терезе,
  фокус, сақтау, бетте жаңарды ✓; нөмір қайтарылды.
- **2026-09-27** · Университет профиліне **«Выйти из аккаунта»** қайтты (бос бет кезінде жоғалған
  еді): карточканың **ең астында** (`flex` баған + `margin-top: auto`), қызыл мәтін + иконка,
  тию аймағы 44px. Растаусыз — ештеңе жоғалмайды.

- **2026-09-27** · Клуб бетіндегі **«Чат» карточкасы тірі чаттың миниатюрасына** айналды
  (пайдаланушының өтініші; бұрын — бір соңғы хабарлама). Соңғы **12 реплика** чаттың өз
  пузырьлерімен (өзіңдікі оңда көк, өзгенікі солда — аты авторлық түспен), **сол тұсқағазда**
  (`multiply` + `--bg-secondary`), лента астына тіреледі, ескілер жоғарғы жиекте `mask-image`-пен
  ериді. Ұзын реплика — 2 жол, фото — «Фото». Опрос чаттағымен бірдей (`POLL_MS`), сондықтан
  жазылып жатқаны көрінеді. «Открыть чат ›» — астында, волоскпен бөлінген жолақ, **ортада** (пайдаланушының өтініші; алдымен оң жаққа қойылған).
  Тексерілді: хабарлама жіберілді → 6 секундта карточкада соңғы пузырь, оң жақта ✓ (өшірілді).
- **2026-09-27** · «События» карточкасы **тазаланды** — сынама «Ярмарка клубов» (2 қазан) өшірілді.

- **2026-09-27** · **«События» беті нөлден қайта жиналады** (пайдаланушының шешімі): карточка мен
  барлық элемент алынды, бет енді **толық енде** (`.page.page--flush`, чат сияқты) және **бос**.
  `Events.css` өшірілді. **Қалдырылды** (қайта жинағанда керек болады): `GET /api/events`
  (жақын іс-шаралар, көріну ережесі чаттағыдай), `api.events`, `events.js` (күн пішімдері —
  клуб бетіндегі «События» карточкасы да қолданады).

- **2026-09-27** · «События» беті ені бойынша **30/70** бөлінді (`.events-layout`, `3fr 7fr`),
  арасында **тік сызық бүкіл биіктікте** (сол панельдің `border-right`, `--separator`).
  Биіктік — `100dvh − --header-height`, бет айналмайды. Екі панель де әзірге бос.

- **2026-09-27** · «События» бетінің жоспары (пайдаланушы түсіндірді): **солда — барлық клубтар
  тізімі, оңда — үлкен күнтізбе**, іс-шаралар сол күнтізбеде. Бірінші қадам — **сол панель**:
  `GET /api/clubs` → әр жолда тек клубтың **квадрат фотосы** (жоқта — әріп) және **аты**, чаттар
  тізімінің жолымен бір стильде (фото кейін **25%-ға үлкейді**: 44 → 55px, әрпі де; клуб аты да сол үлесте: 15 → 18.75px, ұсақ мәтіннің разрядкасы алынды). Сол панель тарылды: 30% → `minmax(16rem, 22rem)` (чат тізімімен
  бірдей), қалғаны күнтізбеге. Тізім өз ішінде айналады, бет айналмайды. Жолдар әзірге ешқайда
  апармайды — күнтізбе келгенде клубты таңдау (сүзгі) болуы мүмкін.

- **2026-09-27** · **Клубтар панелі жиналатын жолақ болды** (пайдаланушының өтініші): тыныш күйде
  ені **фотоның өзіндей** (55px + жолдың шегіністері = `--rail`, 87px), тік сызық фотодан кейін
  бірден тұрады. Курсор апарғанда (не фокус ішінде болса) жолақ **20rem-ге жылжып ашылады**,
  клуб аттары 80ms кешігіп проявляется, астында жұмсақ көлеңке.
  · Жолақ **күнтізбенің үстіне** түседі (`position: absolute`, `z-index`) — тордағы орны тұрақты,
    сондықтан күнтізбе тышқанның әр қимылынан қайта есептелмейді (өлшеп расталды: ашық та,
    жабық та күйде күнтізбе 87px-тен басталады).
  · **§4.3-тен саналы шегініс:** `width` анимацияланады — `transform` болса, волосок шетімен
    бірге жүрмес еді, аттар да шетінде қиылмас еді.
  · Hover жоқ құрылғыда (`@media (hover: none)`) жолақ әрқашан ашық (16rem) — әйтпесе аттарды
    көрудің жолы жоқ. Әр жолда `title` — клуб аты.
- **2026-09-27** · **Ай күнтізбесі** (оң панель, `Calendar` — `Events.jsx` ішінде, Apple «Календарь»
  үлгісі): шапкада **«Сентябрь** 2026» (ай — semibold, жыл — қалыпты салмақ), оң жақта ‹ «Сегодня» ›
  (ағымдағы айда «Сегодня» сөнеді), астында Пн–Вс, сосын **6×7 тор** — әрқашан 6 апта, сондықтан
  биіктігі айдан айға секірмейді. Тор қалған биіктікті тең бөледі, бет айналмайды.
  · Күн нөмірі клетканың оң жоғарғы бұрышында; **бүгін — акцент дөңгелекте**; көрші айлардың
    күндері `--text-tertiary`.
  · Іс-шара — клетка ішінде жол: **клуб түсті нүкте** (`authorColor(club.id)` — чаттағы түспен
    бірдей) → уақыты → атауы (`ellipsis`). Клеткада 3-еуі сыяды, қалғаны «ещё N».
    Жол — сол клубқа сілтеме (`viewTransition`), толық мәлімет `title`-да (атауы — клуб, орны).
  · **Сервер:** `GET /api/events` енді `?from=&to=` қабылдайды — аралық берілсе **өткендері де**
    келеді (күнтізбе тарихты да көрсетеді), берілмесе бұрынғыдай тек болашақтағылар.
    Жарамсыз күн → 400 «Некорректный промежуток дат». Күнтізбе көрініп тұрған 42 күнді
    ғана сұрайды (`api.events({from, to})`). Көріну ережесі өзгермеді: мүшеге — өз клубтары,
    `university`/`admin`-ге — барлығы.
  · Жаңа тәуелділік те, жаңа файл да жоқ: ай аты — `Intl.DateTimeFormat` (атау септігі), уақыт —
    бар `eventTime`.
  Тексерілді: API (аралықсыз — тек болашақ ✓, аралықпен — өткені де ✓, тар аралық ✓,
  жарамсыз күн → 400 ✓, студент-мүше көреді ✓); браузерде 8 сынама іс-шарамен: 7 жол + «ещё 1» ✓,
  бүгін белгіленген ✓, беттің айналымы 0 ✓, жолақ жабық/ашық скриншотпен ✓. Сынама іс-шаралар өшірілді.
  **Әзірге жоқ:** клубты басқанда күнтізбені сүзу, «ещё N»-ді ашу, күнтізбеден іс-шара қосу.

- **2026-09-27** · «Управление клубом» карточкасына **«Принимать заявки»** ауыстырғышы қосылды
  (карточканың ортасы бос тұрды; ұсынылған екі нұсқадан пайдаланушы осыны таңдады).
  · **Бір топта** «Состояние» мен «Приостановить клуб»-пен бірге (пайдаланушының өтініші —
    алдымен бөлек топ болған еді). Астындағы түсіндірме жол **алынды** (пайдаланушының өтініші).
  · **БӘД:** `clubs.accepting boolean not null default true` — жаңа клуб ашық.
    `PATCH /api/clubs/:id {accepting}` (тек boolean, әйтпесе 400; рөлі — `university`/`admin`).
    `POST /api/clubs/:id/members/request` жабық клубқа **403** «Клуб сейчас не принимает заявки» —
    жасырын түйме жеткіліксіз, тыйымды сервер ұстайды. Никнейм бойынша қолмен қосу жұмыс істей береді.
  · Ауыстырғыш бірден жауап береді (сервер артынан, қате болса қайтады) — чаттағы
    «Уведомления»-дағыдай. Басқармайтын адам сол күйді көреді, бірақ тұтқа сөнік (`disabled`).
  · `.switch` стилі енді екі жерде — `Chats.css`-тен **`index.css`-ке** көшті (модификаторы жоқ).
  Тексерілді: API (жабу/ашу ✓, сақталды ✓, boolean емес → 400 ✓, студент → 403 ✓, жабық клубқа
  өтінім → 403 ✓, ашыққа — әдеттегі ережелер ✓, жоқ клуб → 404 ✓); екі рөлде скриншот ✓, айналу 0 ✓.
  **Келесі:** студенттің «Подать заявку» түймесі (Келесі қадам 2) `club.accepting`-ке қарап шығуы керек.

- **2026-09-27** · Клуб бетіндегі **«События» карточкасы бос болды** (пайдаланушының өтініші): тізім,
  «Событий пока нет» және «Добавить событие» формасы алынды, тек көк шапкасы қалды.
  `components/ClubEvents.jsx` пен `.css` **өшірілді** (басқа жерде қолданылмайтын), `events.js`-те тек
  `eventTime` қалды (күнтізбе қолданады). **Қалдырылды:** сервердің `GET|POST|DELETE /api/clubs/:id/events`
  пен `api.clubEvents / createClubEvent / deleteClubEvent` — іс-шара қосу енді күнтізбеден жасалады.
  **Ескерту:** қазір интерфейсте іс-шара қосатын жер **жоқ**.

- **2026-09-27** · «Добавить участника» түймесінің **үстіндегі сызық алынды** (`.side-add`-тің `border-top`-ы,
  пайдаланушының өтініші) — түйме тізімнің астында сызықсыз, тек көк мәтінмен тұрады.
  Өріс ашылғанда оның өз волоскі (`.side-add-form`) бұрынғыдай.

- **2026-09-27** · «Управление участниками» панелі **бір тізім** болды (пайдаланушының өтініші):
  «Руководитель» блогы (көк заливка, бөлек бекітілген жол) **алынды** — барлық мүше бірдей жолмен,
  басшының атының қасында тек көк **«Лидер»** белгісі (сервер оны бірінші қояды). Басшының мәзірі
  («Снять с должности» / «Исключить») сол жолда ашылады.
  · **«Участники · N»** — панельдің **ең үстінде**, іздеудің алдында, анық: 17px semibold `--text`
    (бұрынғы ұсақ сұр бас әріпті `side-title` емес). Сан — **барлық** мүше (басшы да кіреді).
  · «Заявки» бөлімі мүшелер тізімінің **астына** түсті — «Участники» тақырыбы өз тізімінен бөлінбеуі үшін.
  · Өшірілді: `.lead-block`, `.lead*`, `.lead-block .side-title`, `.icon-button--accent` (қолданылмайды).
  · **Бесінші рет CSS реті ұстап алды:** `.row-menu--inline` мен базалық `.row-menu` (`index.css`) бір
    салмақта — мәзір жол ішінде емес, қалқып ашылатын (айналатын тізімде соңғы жолдың мәзірі қиылар еді).
    Селектор `.row-menu.row-menu--inline` қылынды.
  Скриншотпен тексерілді (тізім, басшының ашық мәзірі) ✓

- **2026-09-27** · Мүшелер тізімінің жолы **WhatsApp чаттар тізімі үлгісіне** келтірілді (пайдаланушының
  скриншоты): **48px дөңгелек аватар**, ішінде әріп — адамның **жеке түсімен** (`authorColor`, чаттағы
  атымен бірдей), астары сол түстің 14%-ы (`color-mix(... currentColor 14%, transparent)`); оң жақта
  үстінде **толық аты-жөні** (1rem, 500, сыймаса «…», толығы `title`-де) + «Лидер», астында `@никнейм`
  (сұр). Волоскілер алынды — курсор апарғанда бүкіл жол скруглённый `--fill` плашкамен жанады.
  · Жол бір компонентте — `Person` (`MembersPanel.jsx` ішінде); **өтінімдер де сол жолмен** шығады.
  · Іздеу енді **никнейм бойынша да** (`@` белгісін елемейді).
  Скриншотпен тексерілді (екі мүше + уақытша өтінім; өтінім кейін өшірілді) ✓
- **2026-09-27** · Мүшелер тізімінде: **аты-жөні кішірейді** (1rem → 0.9375rem, никнейм 0.875 → 0.8125rem —
  иерархия сақталды); **«···» мәзірі жол ішінде емес, тізімнің үстіне қалқып** ашылады (пайдаланушының
  өтініші) — `.row-menu--inline` алынды, базалық `.row-menu` (кнопканың астынан). Айналатын тізім
  астынан қиып тастамауы үшін — чаттағыдай: ашылар сәтте `.side-body`-дің шетіне дейінгі орын өлшенеді,
  астында `7.5rem`-ден аз болса әрі үстінде кеңірек болса — **жоғары** ашылады (`.member .row-menu--above`).
  Өлшеп тексерілді: төмен — тізімге сыйды ✓, тар тізімнің соңғы жолында — жоғары ✓. Скриншот ✓
- **2026-09-27** · Мүшелер тізімінің **екі жағындағы артық шегініс алынды** (пайдаланушының өтініші). Себебі —
  `ClubPage.css`-те қалып қойған ескі `.side-body { padding: 16px }` ережесі: сол атаулы класс мүшелер
  панеліндегі тізімге де түсіп, жолды әр жақтан 24px-ке ығыстыратын. Ереже өшірілді (басқа жерде
  қолданылмайтын). Енді жол шеттері іздеу өрісімен бір сызықта (карточка шетінен 8px), іздеу мен тізім
  арасындағы артық 16px те кетті. Өлшеп тексерілді ✓
- **2026-09-27** · Мүшелер панелінің басы **чаттағы «Данные клуба» үлгісіне** келтірілді (пайдаланушы
  «ыңғайсыз» деді; ұсыныс қабылданды): «Участники · N» мен **лупа бір жолда** (`.members-head`), іздеу
  өрісі тұрақты орын алмайды — лупаны басқанда астынан **шығады** (`reveal-y`), фокус бірден соған,
  қайта басқанда жиналып, іздеу тазарады; жиналған өріс `inert` (Tab түспейді).
  · Бұрынғы ақау да кетті: тақырып (16px) мен іздеу капсуласы (12px) бір сызықта емес еді. Енді тақырып
    аватарлармен, лупа — жолдардың оң шетімен дәл тураланған (өлшенді: 1087 / 1503).
  · Лупа стилі екі жерде қолданылады — `Chats.css`-тегі `.chats__icon-button` **`index.css`-ке**
    `.search-toggle` болып көшті (модификаторы жоқ), чаттағы лупа да соны алады.
  Екі күй скриншотпен тексерілді ✓
- **2026-09-27** · Іздеу өрісі енді астынан емес, **сол жолдың ішінде** ашылады (пайдаланушының өтініші):
  лупадан солға қарай, «Участники · N» тақырыбына дейін созылады, тізім **қозғалмайды** (өлшенді: 0px).
  Өріс тақырып пен лупаның арасындағы орынды әрдайым алады (`.members-find`, `flex: 1`), бірақ жабықта
  көрінбейді: `clip-path: inset(0 0 0 100%)` → `inset(0)` + мөлдірлік — маска оңнан солға ашылады,
  макет қайта есептелмейді. Жабықта `inert` әрі `pointer-events: none`. `reveal-y` нұсқасы алынды.
  Тексерілді: фокус өрісте ✓, «@aru» → бір адам ✓. Скриншот ✓
- **2026-09-27** · Ашық іздеуде **екі лупа** қатар тұратын (түймедегі мен өрістегі). Енді түймедегі лупа
  маска шетімен бірге **солға жылжып, кішірейіп, еріп** — өрістің өз лупасына дәл түседі (пайдаланушының
  өтініші; өлшенді: екеуі де 1233×151, 16px). Жол — `translateX(calc(-100cqw + 2.625rem)) scale(0.73)`:
  `.members-find` — контейнер (`container-type: inline-size`), лупа оның енін `cqw` арқылы біледі.
  Өріс енді оң шетке дейін жетеді (жолдармен бір сызықта — 1503), лупа оның үстінде абсолютпен тұрады.
  · Жабатын түйме жоқ болғандықтан — **iOS тәсілі**: `Esc` (фокус лупаға қайтады) немесе **бос** өрістен
    шығу. Жазу бар кезде шықса — ашық қалады (сүзгі көрініп тұрады).
  Тексерілді: Esc ✓, бос өрістен шығу ✓, мәтінмен шығу — ашық ✓. Скриншот (жабық / орта кадр / ашық) ✓
  **Ескерту:** чаттағы «Данные клуба» панелінде лупа әлі ескі тәсілмен (өріс астынан шығады).

- **2026-09-27** · **Шақыру (приглашение) жүйесі** — «Добавить участника» енді адамды бірден қоспайды
  (пайдаланушының шешімі): оған **шақыру** барады, ол **өзі қабылдаса ғана** мүше болады.
  · **БӘД:** бөлек кесте жоқ — шақыру да `club_members` жолы, `status = 'invited'` (өтінім сияқты).
    `check` шектеуі `('active','pending','invited')` болып қайта жазылды, `note` (шақыру мәтіні, ≤300)
    және `invited_by` (кім шақырды, `on delete set null`) бағандары қосылды. Барлық басқа сұраныстар
    (чат, іс-шара, санақ) `status = 'active'` бойынша сүзеді — шақырылған адам ештеңеге кірмейді.
  · **Екі жақтың келісімі = бірден мүше:** өтінім берген адамды шақырса, не шақырылған адам өтінім
    берсе — екінші қадам қажет емес, бірден `active`.
  · **API (басқарушыға):** `GET /api/clubs/:id/candidates?q=` — аты, никнейм не почта бойынша ≤8 адам,
    әрқайсысының клубқа қатынасымен (`active` / `pending` / `invited` / `null`); `%` пен `_` шаблон емес
    (экрандалады); өзін көрсетпейді. `POST /api/clubs/:id/members {userId, note}` — шақыру (қайта →
    409 «Приглашение уже отправлено», мүше → 409 «Этот человек уже в клубе»). `GET /members` жауабында
    `invites` — жауап күтіп тұрғандар; шақыруды қайтару — бұрынғы `DELETE /members/:userId`.
  · **API (шақырылғанға, `routes/invites.js`):** `GET /api/invites` (клуб, кім шақырды, мәтіні),
    `POST /api/invites/:clubId/accept`, `.../decline` (жол өшеді — қайта шақыруға болады).
  · **Интерфейс — басқарушы:** түйме енді **«Пригласить участника»** (не болатынын атайды) → терезе
    `InviteDialog` (нативті `<dialog>`, `.modal`): іздеу (250ms кідіріспен, ≥2 таңба) → нәтижелер мүше
    жолымен (`Person`), «В клубе» / «Приглашён» — сөнік, «Подал заявку» — таңдауға болады → таңдалғаны
    бір жолда + «Изменить» → хабарлама (міндетті емес) → «Пригласить». Панельде жаңа бөлім
    **«Приглашены · N»**, ✕ — шақыруды қайтару. Ескі инлайн форма (`.side-add-form*`) өшірілді.
  · **Интерфейс — шақырылған:** header-де профильдің қасында **қоңырау** (`InvitesBell`) — санағы қызыл
    белгімен; басқанда нативті `popover` тізім (Esc / сыртқа басу жабады, триггерден өседі): клуб
    фотосы → «**AIRec** приглашает вас в клуб», «от …», мәтіні — ақ пузырьда, «Отклонить» / **«Принять»**.
    Қабылдаса — бірден клуб бетіне өтеді. Опрос 30 секунд (қойынды көрінгенде) және терезе фокусқа
    келгенде.
  · `Person` (мүше жолы) жеке файлға шықты — оны панель де, терезе де қолданады.
  · `.modal__button:disabled` стилі қосылды (бұрын сөнік түйме белсендідей көрінетін).
  **Тексерілді:** API — 24 тексеріс (іздеу, `%`, шақыру, қайталау, мүшені шақыру, студент → 403,
  қисық id, ұзын мәтін, шақырылғанның тізімі, бас тарту, қабылдау, чат пайда болды, екі жақты келісім
  екі ретпен) ✓. Браузерде ұштан-ұшқа: терезе, іздеу, таңдау, жіберу → «Приглашены · 1» ✓; уақытша
  аккаунтпен — қоңырауда «1», карточка, «Принять» → клуб беті, белгі кетті ✓. Уақытша аккаунт өшірілді.
  **Ақау болды:** ескі форманы өшіргенде іздеудің `openSearch`/`closeSearch` функциялары да кетіп
  қалған — сборка өтті (анықталмаған атау — тек runtime қатесі), бет ақ болды; браузер консолінен
  табылып, қайтарылды. **Сабақ:** үлкен өшіруден кейін бетті браузерде ашып, консольді тексеру.
  **Әзірге жоқ:** шақыру туралы жүйелік хабарландыру (`Notification`) — тек қоңырау.

- **2026-09-27** · Мүшелер панеліне **навигация** (пайдаланушының өтініші): бір тізімде үш бөлім қатар
  тұрмайды — іздеу жолының астында iOS **сегменттері** «Участники · Приглашены · Заявки», астында тек
  таңдалғанның тізімі. Тақырып таңдалғанды атайды («Приглашены · 1»), іздеу — ашық бөлімнің ішінде,
  бос бөлімнің өз жазуы («Приглашений нет» / «Заявок нет»).
  · Сегменттер: `--fill` астар, таңдалғаны — ақ плашка (`--shadow-knob`), плашка **біреу** әрі
    `transform` арқылы басылғанға жылжиды (`--index`) — қайдан қайда өткені көрінеді. `role="tablist"`,
    `aria-selected`. «Заявки»-де шешім күтетіндер саны — **қызыл белгі**.
  · Сегменттер тек басқарушыға көрінеді — басқаларда тек құрам бар.
  · Шақыру жіберілгенде панель өзі **«Приглашены»**-ге ауысады (өтінім берген адам болса — «Участники»-ге,
    ол бірден мүше болады).
  · `.side-section` (бөлімдердің шегінісі) өшірілді — енді бөлім біреу.
  Үш бөлім уақытша екі аккаунтпен скриншотта тексерілді (плашка орнында ✓, тізімдер ✓); аккаунттар өшірілді.
- **2026-09-27** · Мүше жолында аты-жөні тағы **кішірейді** (пайдаланушы: «тым үлкен»): 15 → **14px**; иерархия сақталуы үшін никнейм мен «Лидер» 13 → **12px**. Бұл өлшемдер шақыру терезесіндегі жолға да түседі (`Person` ортақ).
- **2026-09-27** · Шақыру терезесі **үлкейді** (28 → **36rem**, нәтижелер тізімі 16 → 20rem, хабарлама өрісі 4 жол) және «Начните вводить имя, ник или почту» кеңесі **алынды** (пайдаланушының өтініші) — өрістің өз жазуы жеткілікті; «Никого не нашли» тек іздеу нәтижесіз болғанда шығады.
- **2026-09-27** · Бос тізім жазулары **ортаға** келтірілді (пайдаланушының өтініші): шақыру терезесіндегі «Никого не нашли» (`.invite__hint`) және мүшелер панеліндегі «Никого не нашли» / «Пока никого нет» / «Приглашений нет» / «Заявок нет» / «Загружаем…» (`.side-empty`, үстінен-астынан 24px). Қалғандары (чаттар тізімі, «Данные клуба», клуб бетіндегі «Чат», қоңырау, чат лентасы) бұрыннан ортада еді.

- **2026-09-27** · **Клуб бетіне толық қозғалыс** (`/apple-design` бойынша, пайдаланушының өтініші). Жаңа тәуелділік
  жоқ — бәрі CSS: бетте қимыл (drag/flick) жоқ, сондықтан spring керек емес; transition-дар ағымдағы мәннен
  қайта бағытталады (үзілмелі).
  · **Карточкалар материал болып келеді** (кіру картасындағыдай, §4.4): мөлдірлік + `scale(0.97 → 1)`, 420ms,
    **оқу ретімен** 60ms қадаммен — сведения 0 → управление 60 → участники 120 → чат 180 → события 240ms.
    `animation-fill-mode: backwards` — анимация аяқталған соң араласпайды (чат карточкасының hover-і бұзылмайды).
  · **Кейін жүктелетін мазмұн шықпайды, проявляется** (`@keyframes content-in` — тек мөлдірлік, `index.css`):
    клуб мәліметтері, «Управление клубом» тізімі, мүшелер тізімі.
  · **Күй ауысқанда** («Активен» ↔ «Приостановлен») жол растворением ауысады — `key={club.status}` (екі жерде).
  · **Сегменттер:** басқанда притухает (`:active`, 100ms); бөлім ауысқанда тізім растворением келеді (`key={tab}`).
  · **Мәзір енді кіргендей шығады** (§4.3 симметрия): бұрын жабылғанда бірден жоғалатын. `.row-menu--leaving` +
    `@keyframes row-menu-out` (150ms, түймеге қайта кішірейеді); `MembersPanel` жабылған мәзірді 150ms ұстап тұрады.
  · **Чат карточкасы:** жаңа реплика астынан көтеріліп келеді (`chat-card-msg-in`, лента төменге тіреулі).
  · `prefers-reduced-motion`: кезек пен масштаб алынады (карточкалар бірден), қалғанын глобал ереже қысқартады.
  Тексерілді: анимация параметрлері оқылды (5 карточка, дұрыс кідірістер) ✓, мәзір кету күйі → 300ms-та жоқ ✓,
  бөлім ауысу — `content-in` ✓, reduced-motion — анимация жоқ, бәрі көрінеді ✓, консоль таза ✓.

- **2026-09-27** · **Клуб беті `/apple-design` аудиті бойынша түзетілді** (сегіз табылғанның бәрі, пайдаланушының шешімі):
  1. **Шапкалар көктен ақ тулбарға** (Иерархия): бес бірдей көк плита бір-бірімен таласатын. `.card-header` —
     `--surface` + астында волоск, мәтін `--text`; көк тек әрекеттерде қалды («‹ Клубы», «Редактировать»/«Готово»).
     Чаттағы сол шешім. (Клубтар тізіміндегі карточкалардың көк шапкасы өзгерген жоқ.)
  2. **Қайталау алынды** (Simplicity): бүйір карточка «Управление участниками» → **«Состав клуба»** (студент мұнда
     басқармайды); «Участники · N» тақырыбы басқарушыда алынып, **сандар сегменттердің ішіне** көшті
     («Участники 2 · Приглашены 0 · Заявки ❶»); сегменттер мен лупа — бір жолда, іздеу өрісі сол жолдың үстіне
     лупадан солға қарай ашылады (сегменттер астында ериді, `inert`). Студентте — бұрынғыдай «Участники · N» + лупа.
     Клуб атының астынан күй алынды (ол «Управление клубом»-да, өзгертетін түймесімен бірге) — тек «2 участника».
  3. **Жарық — тек басылатын жолда** (Familiarity): басқарушыда мүше жолы **бүтіні мәзір ашады** (`.member--tap`),
     басқаларда жолда hover жоқ. Мәзір ішін басу жолға өтпейді (`stopPropagation`). Шақыру терезесіндегі
     нұсқаларға hover бөлек берілді.
  4. **«События» бос емес** (Purpose): клубтың **жақын 4 іс-шарасы** (күн плашкасы — сан + ай, атауы, «уақыт · орын»),
     жоқ болса «Ближайших событий нет»; астында **«Открыть календарь ›»** (чаттағыдай жолақ). Жолақ стилі енді
     екеуіне ортақ — `.chat-card__more` → **`.card-more`** (`ClubPage.css`). `events.js`-ке `eventDay`/`eventMonth` қайтты.
  5. **Түс токендері** (§5.4): «‹ Клубы» hover — `--fill`; ✓ түймесі — жаңа `--green-soft` / `--green-strong`.
  6. **«Уменьшить движение»-де растворение қалды** (§14): карточкалар масштабсыз әрі кезексіз, бірақ 160ms ериді;
     глобал ереже нөлдейтін ұзақтық осы беттің тек-мөлдірлік анимацияларына қайтарылды.
  7. **Шапка стилі біреу**: `.side-header*` өшірілді, бүйір карточка да `.card-header`.
  8. **Сегменттің тию аймағы 44px**: өзі 32px (iOS сияқты), `::after` тігінен ±6px кеңейтеді.
  **Тексерілді:** өлшемдер (іздеу өрісі жолдармен бір сызықта 1075–1503 ✓, сегмент 32+6+6 ✓), іс-шаралар тізімі ✓,
  жолды басу → мәзір ✓, студентте сегмент те, жарық та жоқ ✓, консоль таза ✓; уақытша өтінім мен екі іс-шара өшірілді.

- **2026-09-27** · Клуб беті **топталған стильге** көшті (үш нұсқа скриншотпен салыстырылды: жиекті карточкалар /
  сұр фон + жиексіз карточкалар / сызықпен бөлінген толық бет — пайдаланушы ұсынысты таңдады):
  беттің фоны **`--bg-secondary`** (#F5F5F7), карточкалар ақ әрі **жиексіз** — оларды сызық емес, фон контрасты бөледі
  («Настройки», Health сияқты). Сызық азайды: «рамка ішіндегі рамка» кетті, тек шапкалардың астындағы волоск қалды.
  Жаңа модификатор **`.page.page--grouped`** (`Page.css`): сұр фон әрі биіктігі header астындағы бүкіл экран.
  Сызықпен бөлу таңдалмады — клуб бетінің бөліктері тәуелсіз (чаттағыдай тізім → әңгіме емес), бір кестеге айналар еді.
- **2026-09-27** · Курсор апарғанда **барлық карточка** 2%-ға үлкейеді (бұрын тек «Чат»; пайдаланушының өтініші):
  `.club-card` және `.club-page__side`, `transform` қана, `--duration-fast`. Сведения өңделіп жатқанда карточка
  тұрады (`:has(.club-field--editing)`) — теріп жатқан мәтін қозғалмауы үшін. **«События» енді «Чат» сияқты тұтас
  сілтеме** (`/events`), «Открыть календарь» — жай жазу; басқанда кішкене басылу (`:active 0.99`) тек сілтеме
  карточкаларда. Reduced-motion — үлкею жоқ.
  **Ескерту (Familiarity):** басылмайтын карточкалардың (сведения, управление, состав) үлкеюі «басуға болады» деп
  оқылуы мүмкін — пайдаланушыға айтылды.
- **2026-09-27** · Мүшелер панеліндегі **іздеу анимациясы баяулады** (пайдаланушы: «тым тез»): өрістің ашылуы мен
  лупаның жолы 350 → **500ms** (жаңа токен `--duration-slow`), өрістің және сегменттердің еруі 200 → 350ms. Лупаның
  мөлдірлігі енді **`--ease-in`** (жаңа токен) қисығымен — жол бойы көрініп тұрады да, өрістің шетіне жеткенде ериді
  (бұрын `ease-out`-пен басында-ақ жоғалып кететін, жүргені көрінбейтін).

### Келесі қадам
Университет рөлінің минималды жиыны (§ талқыланған): клуб өтінімдерін бекіту,
клубтар тізімі, клуб басшысын тағайындау. Соның ішінде әлі жоқтары:

1. **Клубты кім ашады** деген сұрақ ашық: студент өтінім бере ме (`status='pending'` →
   университет «Одобрить/Отклонить») әлде университеттің өзі құра ма (қазіргі күй).
   Жауап `clubs` кестесіне де, беттегі әрекеттерге де әсер етеді.
2. **Студент жағы**: шақыруды қабылдау/бас тарту — жасалды (2026-09-27, қоңырау). Клуб бетінде «Подать заявку» түймесі жоқ — эндпоинт
   (`POST /api/clubs/:id/members/request`) дайын, UI-ы студент экрандарымен бірге келеді.
   Сонымен бірге: өз өтінімінің күйін көру, клубтан шығу.
3. **Клуб парақшасы** — қаңқасы бар, толық ақпарат пен басқару әлі жоқ.
4. ~~Клуб күйін басқару~~ — жасалды (2026-09-23). Қалғаны: клуб `pending` күйде
   болғанда («заявка») «Одобрить / Отклонить» деген нақты сөзбен шығуы — бұл студент
   клуб ашатын болса ғана керек (1-тармақпен байланысты).
4а. **Басшы тағайындалғанда `users.role` өзгермейді** — адам `student` күйінде қалып,
   `club_members.role = 'lead'` болады. Бір адам бірнеше клубты басқара алатындықтан
   осылай қалдырылды; `club_lead` жаһандық рөлі қайда керек екені шешілмеген.
5. **`Home` беті бос** — не көрсетілетіні шешілмеген.
5а. **Клуб басшысы өз клубын өңдей ала ма?** Әзірге `PATCH` тек `university`/`admin`-ге
   ашық (құрумен бірдей). `club_lead` қосылса, `clubs.lead_id = user.id` тексерілуі керек.
6. ~~Клуб чаты~~ — жасалды (2026-09-23), **фото жіберу** — жасалды (2026-09-26, data URL),
   **медиа мен хабарландыру** — жасалды (2026-09-26).
   Қалғаны: **видео, документ, аудио** — олар data URL-ға сыймайды (1 MB шегі), сондықтан
   файл қоймасы керек (`multer` не таза `express.raw` + дискі) — §2 бойынша сұрау керек.
   Сонымен қатар: оқылмағандар саны; ескі хабарламаларды жүктеу (қазір тек соңғы 50);
   хабарландыру тек қойынды ашық тұрғанда келеді — жабық браузерге push керек
   (Service Worker + VAPID; `web-push` пакеті — §2 бойынша сұрау керек).
   (Өшіру жасалды 2026-09-24; өңдеу әдейі жоқ.)
7. ~~Іс-шаралар~~ — жасалды (2026-09-25), **ай күнтізбесі** — жасалды (2026-09-27).
   Күнтізбеде: клуб бойынша сүзу (жолақтағы клубты басу), «ещё N»-ді ашу, күнтізбеден қосу. Қалғаны: сипаттама мәтіні, қатысамын деп
   белгілеу (RSVP), өткен іс-шараларды бөлек көрсету, клуб басшысының өзі қоса алуы
   (әзірге тек `university`/`admin`).

### Шешілген сұрақтар
- **2026-09-20** · Рөлдер: `admin`, `university`, `club_lead`, `student`.
- **2026-09-20** · Auth: email + пароль, JWT httpOnly cookie-де.
- **2026-09-20** · Иконкалар: Ionicons (`react-icons/io5`), lucide емес.
- **2026-09-20** · Акцент түсі — **логотиптен**: `#0063FD` (`design/logo.png` пиксельдерінен
  өлшенген, ақ фонда 5.0:1). Фон — логотиптің ақ фоны `#FFFFFF`.
- **2026-09-20** · dotenv, cors, bcrypt, ORM, миграция құралы қосылмады —
  сәйкесінше `--env-file`, Vite proxy, `node:crypto`, таза SQL, `sql/setup.js` жеткілікті.

### Ашық сұрақтар
- MVP функционалы нақтыланбаған (клубтар / іс-шаралар / лента / админ-панель).
- «Запомнить меня» әдепкі қосулы. Ортақ компьютерлер үшін өшірулі болуы керек пе —
  пайдаланушы шешеді.
- `Home` әзірге тек «Выйти» түймесінен тұрады (жоғарғы оң жақта). Толық мазмұны — келесі қадам.
- Логин сәтті болғанда уақытша хабар шығады — кабинет беті жасалғанда бағыттау қосылады.

### Ортаға қатысты ескертпе
- Unic порттары: **API `:4400`**, **веб `:5300`**. Әдепкі `4000`/`5173` алынбайды —
  оларды `C:\Nurkeldi\Work\valuai` жобасы иеленіп тұр. Windows екінші `bind`-ты үнсіз
  қабылдайтындықтан, порт қақтығысы қате бермей, сұраныстарды бөгде процеске жібереді.
